// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ui/ui_ast.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package ui;

@kotlin.jvm.JvmName("-initializesliderProps")
public inline fun sliderProps(block: ui.SliderPropsKt.Dsl.() -> kotlin.Unit): ui.UiAst.SliderProps =
  ui.SliderPropsKt.Dsl._create(ui.UiAst.SliderProps.newBuilder()).apply { block() }._build()
/**
 * Protobuf type `ui.SliderProps`
 */
public object SliderPropsKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: ui.UiAst.SliderProps.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: ui.UiAst.SliderProps.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): ui.UiAst.SliderProps = _builder.build()

    /**
     * `int32 min_value = 1;`
     */
    public var minValue: kotlin.Int
      @JvmName("getMinValue")
      get() = _builder.minValue
      @JvmName("setMinValue")
      set(value) {
        _builder.minValue = value
      }
    /**
     * `int32 min_value = 1;`
     */
    public fun clearMinValue() {
      _builder.clearMinValue()
    }

    /**
     * `int32 max_value = 2;`
     */
    public var maxValue: kotlin.Int
      @JvmName("getMaxValue")
      get() = _builder.maxValue
      @JvmName("setMaxValue")
      set(value) {
        _builder.maxValue = value
      }
    /**
     * `int32 max_value = 2;`
     */
    public fun clearMaxValue() {
      _builder.clearMaxValue()
    }

    /**
     * `int32 value = 3;`
     */
    public var value: kotlin.Int
      @JvmName("getValue")
      get() = _builder.value
      @JvmName("setValue")
      set(value) {
        _builder.value = value
      }
    /**
     * `int32 value = 3;`
     */
    public fun clearValue() {
      _builder.clearValue()
    }

    /**
     * `.ui.BarMode mode = 4 [(.buf.validate.field) = { ... }`
     */
    public var mode: ui.UiAst.BarMode
      @JvmName("getMode")
      get() = _builder.mode
      @JvmName("setMode")
      set(value) {
        _builder.mode = value
      }
    public var modeValue: kotlin.Int
      @JvmName("getModeValue")
      get() = _builder.modeValue
      @JvmName("setModeValue")
      set(value) {
        _builder.modeValue = value
      }
    /**
     * `.ui.BarMode mode = 4 [(.buf.validate.field) = { ... }`
     */
    public fun clearMode() {
      _builder.clearMode()
    }

    /**
     * ```
     * Seek immediately on press: LV_EVENT_PRESSED maps the pressed point to a
     * value with the stock update_knob_pos math. Stock LVGL seeks a stationary
     * track tap only at RELEASE, so this changes WHEN the value moves, and
     * nothing else. BAR_MODE_RANGE never press-seeks: which knob a press
     * adjusts is the two-knob proximity contract, and jumping a knob on DOWN
     * would preempt it.
     *
     * THIS PROP IS BEHAVIOUR ONLY. It used to also widen the ext click area to
     * LV_DPX(24), because the wire had nowhere else to put a touch affordance
     * — so a scrubber could not ask for the envelope without the seek, or the
     * seek without the envelope, and no other widget could ask for either.
     * WidgetNode.hit_slop now carries that, for every widget; a press-seek
     * slider that wants the envelope sets both, and its author owes hit_slop's
     * reserve-the-space duty.
     * ```
     *
     * `bool seek_on_press = 5;`
     */
    public var seekOnPress: kotlin.Boolean
      @JvmName("getSeekOnPress")
      get() = _builder.seekOnPress
      @JvmName("setSeekOnPress")
      set(value) {
        _builder.seekOnPress = value
      }
    /**
     * ```
     * Seek immediately on press: LV_EVENT_PRESSED maps the pressed point to a
     * value with the stock update_knob_pos math. Stock LVGL seeks a stationary
     * track tap only at RELEASE, so this changes WHEN the value moves, and
     * nothing else. BAR_MODE_RANGE never press-seeks: which knob a press
     * adjusts is the two-knob proximity contract, and jumping a knob on DOWN
     * would preempt it.
     *
     * THIS PROP IS BEHAVIOUR ONLY. It used to also widen the ext click area to
     * LV_DPX(24), because the wire had nowhere else to put a touch affordance
     * — so a scrubber could not ask for the envelope without the seek, or the
     * seek without the envelope, and no other widget could ask for either.
     * WidgetNode.hit_slop now carries that, for every widget; a press-seek
     * slider that wants the envelope sets both, and its author owes hit_slop's
     * reserve-the-space duty.
     * ```
     *
     * `bool seek_on_press = 5;`
     */
    public fun clearSeekOnPress() {
      _builder.clearSeekOnPress()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun ui.UiAst.SliderProps.copy(block: `ui`.SliderPropsKt.Dsl.() -> kotlin.Unit): ui.UiAst.SliderProps =
  `ui`.SliderPropsKt.Dsl._create(this.toBuilder()).apply { block() }._build()

