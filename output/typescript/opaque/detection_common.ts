// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v5.29.2
// source: opaque/detection_common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** Inference status codes (detector-agnostic). */
export enum DetectionStatus {
  DETECTION_STATUS_UNSPECIFIED = 0,
  /** DETECTION_STATUS_OK - Inference succeeded */
  DETECTION_STATUS_OK = 1,
  /** DETECTION_STATUS_NOT_READY - Engine/IPC not initialized */
  DETECTION_STATUS_NOT_READY = 2,
  /** DETECTION_STATUS_IPC_TIMEOUT - No frame within timeout */
  DETECTION_STATUS_IPC_TIMEOUT = 3,
  /** DETECTION_STATUS_INFER_FAILED - Inference engine error */
  DETECTION_STATUS_INFER_FAILED = 4,
  /** DETECTION_STATUS_ERROR - Unclassified error */
  DETECTION_STATUS_ERROR = 5,
  UNRECOGNIZED = -1,
}

export function detectionStatusFromJSON(object: any): DetectionStatus {
  switch (object) {
    case 0:
    case "DETECTION_STATUS_UNSPECIFIED":
      return DetectionStatus.DETECTION_STATUS_UNSPECIFIED;
    case 1:
    case "DETECTION_STATUS_OK":
      return DetectionStatus.DETECTION_STATUS_OK;
    case 2:
    case "DETECTION_STATUS_NOT_READY":
      return DetectionStatus.DETECTION_STATUS_NOT_READY;
    case 3:
    case "DETECTION_STATUS_IPC_TIMEOUT":
      return DetectionStatus.DETECTION_STATUS_IPC_TIMEOUT;
    case 4:
    case "DETECTION_STATUS_INFER_FAILED":
      return DetectionStatus.DETECTION_STATUS_INFER_FAILED;
    case 5:
    case "DETECTION_STATUS_ERROR":
      return DetectionStatus.DETECTION_STATUS_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DetectionStatus.UNRECOGNIZED;
  }
}

export function detectionStatusToJSON(object: DetectionStatus): string {
  switch (object) {
    case DetectionStatus.DETECTION_STATUS_UNSPECIFIED:
      return "DETECTION_STATUS_UNSPECIFIED";
    case DetectionStatus.DETECTION_STATUS_OK:
      return "DETECTION_STATUS_OK";
    case DetectionStatus.DETECTION_STATUS_NOT_READY:
      return "DETECTION_STATUS_NOT_READY";
    case DetectionStatus.DETECTION_STATUS_IPC_TIMEOUT:
      return "DETECTION_STATUS_IPC_TIMEOUT";
    case DetectionStatus.DETECTION_STATUS_INFER_FAILED:
      return "DETECTION_STATUS_INFER_FAILED";
    case DetectionStatus.DETECTION_STATUS_ERROR:
      return "DETECTION_STATUS_ERROR";
    case DetectionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Single object detection result (detector-agnostic).
 * Bounding box uses NDC coordinates: -1.0 (left/top) to 1.0 (right/bottom), center is (0,0).
 * Consistent with JonGuiDataROI pattern.
 */
export interface ObjectDetection {
  /** Bounding box in NDC (Normalized Device Coordinates) */
  x1: number;
  /** Top edge */
  y1: number;
  /** Right edge */
  x2: number;
  /** Bottom edge */
  y2: number;
  /** Detection confidence */
  confidence: number;
  /** Class ID (0-255, detector-specific meaning) */
  classId: number;
}

/** Inference configuration used for this result. */
export interface DetectionConfig {
  /** Confidence threshold used for filtering */
  confidenceThreshold: number;
  /** NMS IoU threshold used for suppression */
  nmsIouThreshold: number;
}

/** Frame metadata for temporal correlation. */
export interface DetectionFrameMeta {
  /** Presentation timestamp (nanoseconds, must be non-negative) */
  ptsNs: Long;
  /** Capture timestamp (nanoseconds, must be non-negative) */
  captureTimeNs: Long;
  /** Frame generation counter (for correlation with CUDA IPC) */
  generation: number;
  /** Source frame dimensions */
  width: number;
  height: number;
}

function createBaseObjectDetection(): ObjectDetection {
  return { x1: 0, y1: 0, x2: 0, y2: 0, confidence: 0, classId: 0 };
}

export const ObjectDetection: MessageFns<ObjectDetection> = {
  encode(message: ObjectDetection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x1 !== 0) {
      writer.uint32(13).float(message.x1);
    }
    if (message.y1 !== 0) {
      writer.uint32(21).float(message.y1);
    }
    if (message.x2 !== 0) {
      writer.uint32(29).float(message.x2);
    }
    if (message.y2 !== 0) {
      writer.uint32(37).float(message.y2);
    }
    if (message.confidence !== 0) {
      writer.uint32(45).float(message.confidence);
    }
    if (message.classId !== 0) {
      writer.uint32(48).int32(message.classId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectDetection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectDetection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.x1 = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.y1 = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.x2 = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.y2 = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.classId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectDetection {
    return {
      x1: isSet(object.x1) ? globalThis.Number(object.x1) : 0,
      y1: isSet(object.y1) ? globalThis.Number(object.y1) : 0,
      x2: isSet(object.x2) ? globalThis.Number(object.x2) : 0,
      y2: isSet(object.y2) ? globalThis.Number(object.y2) : 0,
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      classId: isSet(object.classId)
        ? globalThis.Number(object.classId)
        : isSet(object.class_id)
        ? globalThis.Number(object.class_id)
        : 0,
    };
  },

  toJSON(message: ObjectDetection): unknown {
    const obj: any = {};
    if (message.x1 !== 0) {
      obj.x1 = message.x1;
    }
    if (message.y1 !== 0) {
      obj.y1 = message.y1;
    }
    if (message.x2 !== 0) {
      obj.x2 = message.x2;
    }
    if (message.y2 !== 0) {
      obj.y2 = message.y2;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.classId !== 0) {
      obj.classId = Math.round(message.classId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectDetection>, I>>(base?: I): ObjectDetection {
    return ObjectDetection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectDetection>, I>>(object: I): ObjectDetection {
    const message = createBaseObjectDetection();
    message.x1 = object.x1 ?? 0;
    message.y1 = object.y1 ?? 0;
    message.x2 = object.x2 ?? 0;
    message.y2 = object.y2 ?? 0;
    message.confidence = object.confidence ?? 0;
    message.classId = object.classId ?? 0;
    return message;
  },
};

function createBaseDetectionConfig(): DetectionConfig {
  return { confidenceThreshold: 0, nmsIouThreshold: 0 };
}

export const DetectionConfig: MessageFns<DetectionConfig> = {
  encode(message: DetectionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.confidenceThreshold !== 0) {
      writer.uint32(13).float(message.confidenceThreshold);
    }
    if (message.nmsIouThreshold !== 0) {
      writer.uint32(21).float(message.nmsIouThreshold);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.confidenceThreshold = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.nmsIouThreshold = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectionConfig {
    return {
      confidenceThreshold: isSet(object.confidenceThreshold)
        ? globalThis.Number(object.confidenceThreshold)
        : isSet(object.confidence_threshold)
        ? globalThis.Number(object.confidence_threshold)
        : 0,
      nmsIouThreshold: isSet(object.nmsIouThreshold)
        ? globalThis.Number(object.nmsIouThreshold)
        : isSet(object.nms_iou_threshold)
        ? globalThis.Number(object.nms_iou_threshold)
        : 0,
    };
  },

  toJSON(message: DetectionConfig): unknown {
    const obj: any = {};
    if (message.confidenceThreshold !== 0) {
      obj.confidenceThreshold = message.confidenceThreshold;
    }
    if (message.nmsIouThreshold !== 0) {
      obj.nmsIouThreshold = message.nmsIouThreshold;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectionConfig>, I>>(base?: I): DetectionConfig {
    return DetectionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectionConfig>, I>>(object: I): DetectionConfig {
    const message = createBaseDetectionConfig();
    message.confidenceThreshold = object.confidenceThreshold ?? 0;
    message.nmsIouThreshold = object.nmsIouThreshold ?? 0;
    return message;
  },
};

function createBaseDetectionFrameMeta(): DetectionFrameMeta {
  return { ptsNs: Long.UZERO, captureTimeNs: Long.UZERO, generation: 0, width: 0, height: 0 };
}

export const DetectionFrameMeta: MessageFns<DetectionFrameMeta> = {
  encode(message: DetectionFrameMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.ptsNs.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.ptsNs.toString());
    }
    if (!message.captureTimeNs.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.captureTimeNs.toString());
    }
    if (message.generation !== 0) {
      writer.uint32(24).uint32(message.generation);
    }
    if (message.width !== 0) {
      writer.uint32(32).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(40).uint32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectionFrameMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectionFrameMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ptsNs = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.captureTimeNs = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.generation = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectionFrameMeta {
    return {
      ptsNs: isSet(object.ptsNs)
        ? Long.fromValue(object.ptsNs)
        : isSet(object.pts_ns)
        ? Long.fromValue(object.pts_ns)
        : Long.UZERO,
      captureTimeNs: isSet(object.captureTimeNs)
        ? Long.fromValue(object.captureTimeNs)
        : isSet(object.capture_time_ns)
        ? Long.fromValue(object.capture_time_ns)
        : Long.UZERO,
      generation: isSet(object.generation) ? globalThis.Number(object.generation) : 0,
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: DetectionFrameMeta): unknown {
    const obj: any = {};
    if (!message.ptsNs.equals(Long.UZERO)) {
      obj.ptsNs = (message.ptsNs || Long.UZERO).toString();
    }
    if (!message.captureTimeNs.equals(Long.UZERO)) {
      obj.captureTimeNs = (message.captureTimeNs || Long.UZERO).toString();
    }
    if (message.generation !== 0) {
      obj.generation = Math.round(message.generation);
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectionFrameMeta>, I>>(base?: I): DetectionFrameMeta {
    return DetectionFrameMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectionFrameMeta>, I>>(object: I): DetectionFrameMeta {
    const message = createBaseDetectionFrameMeta();
    message.ptsNs = (object.ptsNs !== undefined && object.ptsNs !== null) ? Long.fromValue(object.ptsNs) : Long.UZERO;
    message.captureTimeNs = (object.captureTimeNs !== undefined && object.captureTimeNs !== null)
      ? Long.fromValue(object.captureTimeNs)
      : Long.UZERO;
    message.generation = object.generation ?? 0;
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
