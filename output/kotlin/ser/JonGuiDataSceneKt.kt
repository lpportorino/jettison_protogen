// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: jon_shared_data_scene.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package ser;

@kotlin.jvm.JvmName("-initializejonGuiDataScene")
public inline fun jonGuiDataScene(block: ser.JonGuiDataSceneKt.Dsl.() -> kotlin.Unit): ser.JonSharedDataScene.JonGuiDataScene =
  ser.JonGuiDataSceneKt.Dsl._create(ser.JonSharedDataScene.JonGuiDataScene.newBuilder()).apply { block() }._build()
/**
 * ```
 * What the FULL-AUTO scene classifier thinks the world is, per channel.
 *
 * Published every state tick by eutropia's `Isp3aHost` from the `scene_day`
 * guest's output block (`mods/isp3a/scene/`) — the third sandboxed 3A guest,
 * beside `ae_day` and `awb_day`. It is a REPORT, never a control surface: the
 * operator's own latch is `cmd.DayCamera.SceneAuto` / `cmd.HeatCamera.SceneAuto`
 * and the mode the pipeline actually runs stays `camera_day.fx_mode` /
 * `camera_heat.fx_mode`.
 *
 * ⚠ READ `shadow` FIRST. While it is true the guest computes, publishes and
 * logs, and drives NOTHING — no `reload_params`, no mode change. `day_mode` is
 * then the mode it WOULD select, not one it has caused, and an operator seeing
 * a `day_class` that disagrees with the running `fx_mode` is seeing the system
 * working as designed. Every other field means the same thing in both regimes,
 * which is what makes the shadow judgement comparable with the acting one.
 *
 * A class scores 0..1 per tick; the incumbent (`day_class`) changes only when
 * a challenger (`day_challenger`) beats it by a margin for a dwell, and the
 * guest never evaluates at all during an AE or AWB transient. `day_hold_s` is
 * how long the current challenger has been holding and `day_hold_reason` says
 * which rule is holding it, so a UI can show a pending switch rather than a
 * silent one.
 * ```
 *
 * Protobuf type `ser.JonGuiDataScene`
 */
public object JonGuiDataSceneKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: ser.JonSharedDataScene.JonGuiDataScene.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: ser.JonSharedDataScene.JonGuiDataScene.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): ser.JonSharedDataScene.JonGuiDataScene = _builder.build()

    /**
     * ```
     * The operator's day full-auto latch (`cmd.DayCamera.SceneAuto.enable`).
     *
     * While this is false the classifier still runs and still publishes — it is
     * a scene report, not a mode driver — so the scores below are live either
     * way. What the latch gates is the ACT: with it on, and the guest's report
     * settled, the HOST emits `cmd.DayCamera.SetFxMode` for `day_mode`. With it
     * off nothing is commanded, and `shadow` says so.
     * ```
     *
     * `bool day_auto = 1;`
     */
    public var dayAuto: kotlin.Boolean
      @JvmName("getDayAuto")
      get() = _builder.dayAuto
      @JvmName("setDayAuto")
      set(value) {
        _builder.dayAuto = value
      }
    /**
     * ```
     * The operator's day full-auto latch (`cmd.DayCamera.SceneAuto.enable`).
     *
     * While this is false the classifier still runs and still publishes — it is
     * a scene report, not a mode driver — so the scores below are live either
     * way. What the latch gates is the ACT: with it on, and the guest's report
     * settled, the HOST emits `cmd.DayCamera.SetFxMode` for `day_mode`. With it
     * off nothing is commanded, and `shadow` says so.
     * ```
     *
     * `bool day_auto = 1;`
     */
    public fun clearDayAuto() {
      _builder.clearDayAuto()
    }

    /**
     * ```
     * The class the guest currently holds for the day channel. UNSPECIFIED means
     * it has no usable input (see `day_hold_reason`), not "no scene".
     * ```
     *
     * `.ser.JonGuiDataSceneClass day_class = 2 [(.buf.validate.field) = { ... }`
     */
    public var dayClass: ser.JonSharedDataTypes.JonGuiDataSceneClass
      @JvmName("getDayClass")
      get() = _builder.dayClass
      @JvmName("setDayClass")
      set(value) {
        _builder.dayClass = value
      }
    public var dayClassValue: kotlin.Int
      @JvmName("getDayClassValue")
      get() = _builder.dayClassValue
      @JvmName("setDayClassValue")
      set(value) {
        _builder.dayClassValue = value
      }
    /**
     * ```
     * The class the guest currently holds for the day channel. UNSPECIFIED means
     * it has no usable input (see `day_hold_reason`), not "no scene".
     * ```
     *
     * `.ser.JonGuiDataSceneClass day_class = 2 [(.buf.validate.field) = { ... }`
     */
    public fun clearDayClass() {
      _builder.clearDayClass()
    }

    /**
     * ```
     * The class currently beating the incumbent, if any — UNSPECIFIED when the
     * incumbent is also the best-scoring class. A UI shows this as a pending
     * switch; it is NOT a decision.
     * ```
     *
     * `.ser.JonGuiDataSceneClass day_challenger = 3 [(.buf.validate.field) = { ... }`
     */
    public var dayChallenger: ser.JonSharedDataTypes.JonGuiDataSceneClass
      @JvmName("getDayChallenger")
      get() = _builder.dayChallenger
      @JvmName("setDayChallenger")
      set(value) {
        _builder.dayChallenger = value
      }
    public var dayChallengerValue: kotlin.Int
      @JvmName("getDayChallengerValue")
      get() = _builder.dayChallengerValue
      @JvmName("setDayChallengerValue")
      set(value) {
        _builder.dayChallengerValue = value
      }
    /**
     * ```
     * The class currently beating the incumbent, if any — UNSPECIFIED when the
     * incumbent is also the best-scoring class. A UI shows this as a pending
     * switch; it is NOT a decision.
     * ```
     *
     * `.ser.JonGuiDataSceneClass day_challenger = 3 [(.buf.validate.field) = { ... }`
     */
    public fun clearDayChallenger() {
      _builder.clearDayChallenger()
    }

    /**
     * ```
     * How long the challenger has held its lead, in whole seconds. 0 when there
     * is no challenger. Reaching the configured dwell is what promotes it.
     * ```
     *
     * `uint32 day_hold_s = 4;`
     */
    public var dayHoldS: kotlin.Int
      @JvmName("getDayHoldS")
      get() = _builder.dayHoldS
      @JvmName("setDayHoldS")
      set(value) {
        _builder.dayHoldS = value
      }
    /**
     * ```
     * How long the challenger has held its lead, in whole seconds. 0 when there
     * is no challenger. Reaching the configured dwell is what promotes it.
     * ```
     *
     * `uint32 day_hold_s = 4;`
     */
    public fun clearDayHoldS() {
      _builder.clearDayHoldS()
    }

    /**
     * ```
     * WHY the incumbent has not changed, as a short stable ASCII token, never
     * prose: one of `ok` (no challenger), `dwell` (a challenger is leading but
     * has not held long enough), `margin` (leading by less than the margin),
     * `transient` (an AE or AWB transient — the guest does not evaluate at all),
     * `ratelimit` (a switch happened too recently), `noinput` (no usable
     * statistics), `init` (nothing decided yet). The vocabulary is the guest's
     * (`mods/isp3a/scene/scene.c`), written straight into the ABI rather than
     * mapped by the host, so there is no second copy of it to drift.
     * ```
     *
     * `string day_hold_reason = 5 [(.buf.validate.field) = { ... }`
     */
    public var dayHoldReason: kotlin.String
      @JvmName("getDayHoldReason")
      get() = _builder.dayHoldReason
      @JvmName("setDayHoldReason")
      set(value) {
        _builder.dayHoldReason = value
      }
    /**
     * ```
     * WHY the incumbent has not changed, as a short stable ASCII token, never
     * prose: one of `ok` (no challenger), `dwell` (a challenger is leading but
     * has not held long enough), `margin` (leading by less than the margin),
     * `transient` (an AE or AWB transient — the guest does not evaluate at all),
     * `ratelimit` (a switch happened too recently), `noinput` (no usable
     * statistics), `init` (nothing decided yet). The vocabulary is the guest's
     * (`mods/isp3a/scene/scene.c`), written straight into the ABI rather than
     * mapped by the host, so there is no second copy of it to drift.
     * ```
     *
     * `string day_hold_reason = 5 [(.buf.validate.field) = { ... }`
     */
    public fun clearDayHoldReason() {
      _builder.clearDayHoldReason()
    }

    /**
     * ```
     * The day FX mode this class maps to.
     *
     * Read it with `shadow`: while `shadow` is true this is what auto WOULD
     * select and nothing is driving it, and while `shadow` is false it is what
     * auto is driving the camera toward. Either way the RUNNING mode stays
     * `camera_day.fx_mode` — this field is never a readback.
     *
     * `*_DEFAULT` (0) is legal here, unlike in `cmd.DayCamera.SetFxMode`: a
     * publish before the first decision legitimately reports the default.
     * ```
     *
     * `.ser.JonGuiDataFxModeDay day_mode = 6 [(.buf.validate.field) = { ... }`
     */
    public var dayMode: ser.JonSharedDataTypes.JonGuiDataFxModeDay
      @JvmName("getDayMode")
      get() = _builder.dayMode
      @JvmName("setDayMode")
      set(value) {
        _builder.dayMode = value
      }
    public var dayModeValue: kotlin.Int
      @JvmName("getDayModeValue")
      get() = _builder.dayModeValue
      @JvmName("setDayModeValue")
      set(value) {
        _builder.dayModeValue = value
      }
    /**
     * ```
     * The day FX mode this class maps to.
     *
     * Read it with `shadow`: while `shadow` is true this is what auto WOULD
     * select and nothing is driving it, and while `shadow` is false it is what
     * auto is driving the camera toward. Either way the RUNNING mode stays
     * `camera_day.fx_mode` — this field is never a readback.
     *
     * `*_DEFAULT` (0) is legal here, unlike in `cmd.DayCamera.SetFxMode`: a
     * publish before the first decision legitimately reports the default.
     * ```
     *
     * `.ser.JonGuiDataFxModeDay day_mode = 6 [(.buf.validate.field) = { ... }`
     */
    public fun clearDayMode() {
      _builder.clearDayMode()
    }

    /**
     * An uninstantiable, behaviorless type to represent the field in
     * generics.
     */
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    public class DayScoresProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
    /**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     */
     public val dayScores: com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>
      @kotlin.jvm.JvmSynthetic
      get() = com.google.protobuf.kotlin.DslList(
        _builder.dayScoresList
      )
    /**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     * @param value The dayScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addDayScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.add(value: kotlin.Float) {
      _builder.addDayScores(value)
    }/**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     * @param value The dayScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignDayScores")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.plusAssign(value: kotlin.Float) {
      add(value)
    }/**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     * @param values The dayScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addAllDayScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.addAll(values: kotlin.collections.Iterable<kotlin.Float>) {
      _builder.addAllDayScores(values)
    }/**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     * @param values The dayScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignAllDayScores")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.plusAssign(values: kotlin.collections.Iterable<kotlin.Float>) {
      addAll(values)
    }/**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     * @param index The index to set the value at.
     * @param value The dayScores to set.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("setDayScores")
    public operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.set(index: kotlin.Int, value: kotlin.Float) {
      _builder.setDayScores(index, value)
    }/**
     * ```
     * One score per day class in `JonGuiDataSceneClass` order, starting at DAY —
     * index 0 = DAY, 1 = DUSK, 2 = NIGHT, 3 = FOG, 4 = OVERCAST. UNSPECIFIED is
     * never scored, so the list is one shorter than the enum.
     *
     * EMPTY (not zeroed) when the guest scored nothing this tick: a vector of
     * zeros is a measurement and absence is not, and the two must not print the
     * same. `day_hold_reason` then says why.
     * ```
     *
     * `repeated float day_scores = 7 [(.buf.validate.field) = { ... }`
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("clearDayScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, DayScoresProxy>.clear() {
      _builder.clearDayScores()
    }
    /**
     * ```
     * The operator's heat full-auto latch (`cmd.HeatCamera.SceneAuto.enable`).
     * ```
     *
     * `bool heat_auto = 8;`
     */
    public var heatAuto: kotlin.Boolean
      @JvmName("getHeatAuto")
      get() = _builder.heatAuto
      @JvmName("setHeatAuto")
      set(value) {
        _builder.heatAuto = value
      }
    /**
     * ```
     * The operator's heat full-auto latch (`cmd.HeatCamera.SceneAuto.enable`).
     * ```
     *
     * `bool heat_auto = 8;`
     */
    public fun clearHeatAuto() {
      _builder.clearHeatAuto()
    }

    /**
     * ```
     * The class the guest currently holds for the thermal channel.
     *
     * ⚠ UNSPECIFIED with `heat_hold_reason` = `noinput` is the EXPECTED state on
     * every box today, and it is a statement about the telemetry rather than
     * about the scene: the pre-AGC signal the heat classifier needs is not
     * published by anything (see `JonGuiDataHeatSceneClass`). Do not read it as
     * a fault, and do not fill it from post-AGC contrast.
     * ```
     *
     * `.ser.JonGuiDataHeatSceneClass heat_class = 9 [(.buf.validate.field) = { ... }`
     */
    public var heatClass: ser.JonSharedDataTypes.JonGuiDataHeatSceneClass
      @JvmName("getHeatClass")
      get() = _builder.heatClass
      @JvmName("setHeatClass")
      set(value) {
        _builder.heatClass = value
      }
    public var heatClassValue: kotlin.Int
      @JvmName("getHeatClassValue")
      get() = _builder.heatClassValue
      @JvmName("setHeatClassValue")
      set(value) {
        _builder.heatClassValue = value
      }
    /**
     * ```
     * The class the guest currently holds for the thermal channel.
     *
     * ⚠ UNSPECIFIED with `heat_hold_reason` = `noinput` is the EXPECTED state on
     * every box today, and it is a statement about the telemetry rather than
     * about the scene: the pre-AGC signal the heat classifier needs is not
     * published by anything (see `JonGuiDataHeatSceneClass`). Do not read it as
     * a fault, and do not fill it from post-AGC contrast.
     * ```
     *
     * `.ser.JonGuiDataHeatSceneClass heat_class = 9 [(.buf.validate.field) = { ... }`
     */
    public fun clearHeatClass() {
      _builder.clearHeatClass()
    }

    /**
     * ```
     * The thermal class beating the incumbent, if any.
     * ```
     *
     * `.ser.JonGuiDataHeatSceneClass heat_challenger = 10 [(.buf.validate.field) = { ... }`
     */
    public var heatChallenger: ser.JonSharedDataTypes.JonGuiDataHeatSceneClass
      @JvmName("getHeatChallenger")
      get() = _builder.heatChallenger
      @JvmName("setHeatChallenger")
      set(value) {
        _builder.heatChallenger = value
      }
    public var heatChallengerValue: kotlin.Int
      @JvmName("getHeatChallengerValue")
      get() = _builder.heatChallengerValue
      @JvmName("setHeatChallengerValue")
      set(value) {
        _builder.heatChallengerValue = value
      }
    /**
     * ```
     * The thermal class beating the incumbent, if any.
     * ```
     *
     * `.ser.JonGuiDataHeatSceneClass heat_challenger = 10 [(.buf.validate.field) = { ... }`
     */
    public fun clearHeatChallenger() {
      _builder.clearHeatChallenger()
    }

    /**
     * ```
     * How long the thermal challenger has held its lead, in whole seconds.
     * ```
     *
     * `uint32 heat_hold_s = 11;`
     */
    public var heatHoldS: kotlin.Int
      @JvmName("getHeatHoldS")
      get() = _builder.heatHoldS
      @JvmName("setHeatHoldS")
      set(value) {
        _builder.heatHoldS = value
      }
    /**
     * ```
     * How long the thermal challenger has held its lead, in whole seconds.
     * ```
     *
     * `uint32 heat_hold_s = 11;`
     */
    public fun clearHeatHoldS() {
      _builder.clearHeatHoldS()
    }

    /**
     * ```
     * Why the thermal incumbent has not changed — the same token vocabulary as
     * `day_hold_reason`.
     * ```
     *
     * `string heat_hold_reason = 12 [(.buf.validate.field) = { ... }`
     */
    public var heatHoldReason: kotlin.String
      @JvmName("getHeatHoldReason")
      get() = _builder.heatHoldReason
      @JvmName("setHeatHoldReason")
      set(value) {
        _builder.heatHoldReason = value
      }
    /**
     * ```
     * Why the thermal incumbent has not changed — the same token vocabulary as
     * `day_hold_reason`.
     * ```
     *
     * `string heat_hold_reason = 12 [(.buf.validate.field) = { ... }`
     */
    public fun clearHeatHoldReason() {
      _builder.clearHeatHoldReason()
    }

    /**
     * ```
     * The heat FX mode this class maps to — what the guest WOULD select.
     * ```
     *
     * `.ser.JonGuiDataFxModeHeat heat_mode = 13 [(.buf.validate.field) = { ... }`
     */
    public var heatMode: ser.JonSharedDataTypes.JonGuiDataFxModeHeat
      @JvmName("getHeatMode")
      get() = _builder.heatMode
      @JvmName("setHeatMode")
      set(value) {
        _builder.heatMode = value
      }
    public var heatModeValue: kotlin.Int
      @JvmName("getHeatModeValue")
      get() = _builder.heatModeValue
      @JvmName("setHeatModeValue")
      set(value) {
        _builder.heatModeValue = value
      }
    /**
     * ```
     * The heat FX mode this class maps to — what the guest WOULD select.
     * ```
     *
     * `.ser.JonGuiDataFxModeHeat heat_mode = 13 [(.buf.validate.field) = { ... }`
     */
    public fun clearHeatMode() {
      _builder.clearHeatMode()
    }

    /**
     * An uninstantiable, behaviorless type to represent the field in
     * generics.
     */
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    public class HeatScoresProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
    /**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     */
     public val heatScores: com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>
      @kotlin.jvm.JvmSynthetic
      get() = com.google.protobuf.kotlin.DslList(
        _builder.heatScoresList
      )
    /**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     * @param value The heatScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addHeatScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.add(value: kotlin.Float) {
      _builder.addHeatScores(value)
    }/**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     * @param value The heatScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignHeatScores")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.plusAssign(value: kotlin.Float) {
      add(value)
    }/**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     * @param values The heatScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addAllHeatScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.addAll(values: kotlin.collections.Iterable<kotlin.Float>) {
      _builder.addAllHeatScores(values)
    }/**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     * @param values The heatScores to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignAllHeatScores")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.plusAssign(values: kotlin.collections.Iterable<kotlin.Float>) {
      addAll(values)
    }/**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     * @param index The index to set the value at.
     * @param value The heatScores to set.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("setHeatScores")
    public operator fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.set(index: kotlin.Int, value: kotlin.Float) {
      _builder.setHeatScores(index, value)
    }/**
     * ```
     * One score per thermal class in enum order, starting at HIGH_CONTRAST —
     * index 0 = HIGH_CONTRAST, 1 = LOW_CONTRAST. EMPTY when nothing was scored.
     * ```
     *
     * `repeated float heat_scores = 14 [(.buf.validate.field) = { ... }`
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("clearHeatScores")
    public fun com.google.protobuf.kotlin.DslList<kotlin.Float, HeatScoresProxy>.clear() {
      _builder.clearHeatScores()
    }
    /**
     * ```
     * NOTHING IS ACTING ON THE PICTURE. True while neither channel is armed,
     * false as soon as one is — where a channel is armed when its `*_auto` latch
     * is on AND the guest is not reporting `noinput` for it.
     *
     * So `shadow` false means some mode on this device is being chosen by the
     * classifier rather than by the operator, and a UI must render the modes as
     * automatic. `shadow` true means every mode is whatever was last set by
     * hand, whichever way the latches read: the thermal half reports `noinput`
     * on every box today (nothing publishes the pre-AGC drive it needs), so
     * turning the heat latch on by itself does NOT clear this bit.
     *
     * ⚠ IT IS NOT THE GUEST'S OWN SHADOW BIT. The WASM classifier has no act
     * path at all — it emits no command record and no `reload_params`, and
     * asserts `ISP3A_OF_SCENE_SHADOW` about itself unconditionally on every
     * build there is. Publishing that bit here would read as "nothing is
     * acting" on a device whose mode the host is driving, which is the single
     * most misleading thing this block could say. The guest's own bit is not
     * lost — it rides eutropia's `3A scene` log line as `guest_shadow`, beside
     * the host's `day_armed` / `heat_armed`.
     * ```
     *
     * `bool shadow = 15;`
     */
    public var shadow: kotlin.Boolean
      @JvmName("getShadow")
      get() = _builder.shadow
      @JvmName("setShadow")
      set(value) {
        _builder.shadow = value
      }
    /**
     * ```
     * NOTHING IS ACTING ON THE PICTURE. True while neither channel is armed,
     * false as soon as one is — where a channel is armed when its `*_auto` latch
     * is on AND the guest is not reporting `noinput` for it.
     *
     * So `shadow` false means some mode on this device is being chosen by the
     * classifier rather than by the operator, and a UI must render the modes as
     * automatic. `shadow` true means every mode is whatever was last set by
     * hand, whichever way the latches read: the thermal half reports `noinput`
     * on every box today (nothing publishes the pre-AGC drive it needs), so
     * turning the heat latch on by itself does NOT clear this bit.
     *
     * ⚠ IT IS NOT THE GUEST'S OWN SHADOW BIT. The WASM classifier has no act
     * path at all — it emits no command record and no `reload_params`, and
     * asserts `ISP3A_OF_SCENE_SHADOW` about itself unconditionally on every
     * build there is. Publishing that bit here would read as "nothing is
     * acting" on a device whose mode the host is driving, which is the single
     * most misleading thing this block could say. The guest's own bit is not
     * lost — it rides eutropia's `3A scene` log line as `guest_shadow`, beside
     * the host's `day_armed` / `heat_armed`.
     * ```
     *
     * `bool shadow = 15;`
     */
    public fun clearShadow() {
      _builder.clearShadow()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun ser.JonSharedDataScene.JonGuiDataScene.copy(block: `ser`.JonGuiDataSceneKt.Dsl.() -> kotlin.Unit): ser.JonSharedDataScene.JonGuiDataScene =
  `ser`.JonGuiDataSceneKt.Dsl._create(this.toBuilder()).apply { block() }._build()

