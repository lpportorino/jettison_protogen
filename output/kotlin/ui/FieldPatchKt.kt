// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ui/ui_ast.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package ui;

@kotlin.jvm.JvmName("-initializefieldPatch")
public inline fun fieldPatch(block: ui.FieldPatchKt.Dsl.() -> kotlin.Unit): ui.UiAst.FieldPatch =
  ui.FieldPatchKt.Dsl._create(ui.UiAst.FieldPatch.newBuilder()).apply { block() }._build()
/**
 * ```
 * One fixed-width slot in a CmdSpec.root_template the renderer overwrites.
 * ```
 *
 * Protobuf type `ui.FieldPatch`
 */
public object FieldPatchKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: ui.UiAst.FieldPatch.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: ui.UiAst.FieldPatch.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): ui.UiAst.FieldPatch = _builder.build()

    /**
     * ```
     * start of the slot in root_template
     * ```
     *
     * `uint32 byte_offset = 1;`
     */
    public var byteOffset: kotlin.Int
      @JvmName("getByteOffset")
      get() = _builder.byteOffset
      @JvmName("setByteOffset")
      set(value) {
        _builder.byteOffset = value
      }
    /**
     * ```
     * start of the slot in root_template
     * ```
     *
     * `uint32 byte_offset = 1;`
     */
    public fun clearByteOffset() {
      _builder.clearByteOffset()
    }

    /**
     * ```
     * slot width (8 double, 4 float, 5/10 padded varint)
     * ```
     *
     * `uint32 byte_width = 2;`
     */
    public var byteWidth: kotlin.Int
      @JvmName("getByteWidth")
      get() = _builder.byteWidth
      @JvmName("setByteWidth")
      set(value) {
        _builder.byteWidth = value
      }
    /**
     * ```
     * slot width (8 double, 4 float, 5/10 padded varint)
     * ```
     *
     * `uint32 byte_width = 2;`
     */
    public fun clearByteWidth() {
      _builder.clearByteWidth()
    }

    /**
     * `.ui.PatchKind kind = 3 [(.buf.validate.field) = { ... }`
     */
    public var kind: ui.UiAst.PatchKind
      @JvmName("getKind")
      get() = _builder.kind
      @JvmName("setKind")
      set(value) {
        _builder.kind = value
      }
    public var kindValue: kotlin.Int
      @JvmName("getKindValue")
      get() = _builder.kindValue
      @JvmName("setKindValue")
      set(value) {
        _builder.kindValue = value
      }
    /**
     * `.ui.PatchKind kind = 3 [(.buf.validate.field) = { ... }`
     */
    public fun clearKind() {
      _builder.clearKind()
    }

    /**
     * ```
     * gen-time fixed-point factor (uigen.scales), whose ONE definition is
     * `proto value × wire_scale = the ABI int` the LVGL widgets and subjects
     * ride. The float/double encodings therefore DIVIDE by it to recover the
     * proto value; the padded varint MULTIPLIES, because an integer leaf's ABI
     * int is already in the proto's own unit and its scale is 1. A wire_scale
     * ≤ 0 is refused rather than applied — it can only come from a malformed
     * producer, and both directions would silently corrupt the slot.
     * ```
     *
     * `sint32 wire_scale = 4;`
     */
    public var wireScale: kotlin.Int
      @JvmName("getWireScale")
      get() = _builder.wireScale
      @JvmName("setWireScale")
      set(value) {
        _builder.wireScale = value
      }
    /**
     * ```
     * gen-time fixed-point factor (uigen.scales), whose ONE definition is
     * `proto value × wire_scale = the ABI int` the LVGL widgets and subjects
     * ride. The float/double encodings therefore DIVIDE by it to recover the
     * proto value; the padded varint MULTIPLIES, because an integer leaf's ABI
     * int is already in the proto's own unit and its scale is 1. A wire_scale
     * ≤ 0 is refused rather than applied — it can only come from a malformed
     * producer, and both directions would silently corrupt the slot.
     * ```
     *
     * `sint32 wire_scale = 4;`
     */
    public fun clearWireScale() {
      _builder.clearWireScale()
    }

    /**
     * ```
     * The local subject whose current int this slot reads. REQUIRED when kind is
     * PATCH_KIND_SUBJECT_VALUE and MUST be empty for every other kind; the
     * renderer refuses both violations. Bounded at 63 like every other subject
     * reference (SubjectDeclaration.name, VisibilityBinding.subject,
     * EventBinding.set_subject), so a name legal to DECLARE is always legal to
     * reference here. A NAME rather than a registry index deliberately: the
     * renderer's registry order is a decode-time implementation detail that a
     * dropped or reordered declaration silently shifts, so an index would
     * resolve to the WRONG subject and send a plausible wrong value, while an
     * unresolvable name fails loud at load.
     * ```
     *
     * `string subject = 5 [(.buf.validate.field) = { ... }`
     */
    public var subject: kotlin.String
      @JvmName("getSubject")
      get() = _builder.subject
      @JvmName("setSubject")
      set(value) {
        _builder.subject = value
      }
    /**
     * ```
     * The local subject whose current int this slot reads. REQUIRED when kind is
     * PATCH_KIND_SUBJECT_VALUE and MUST be empty for every other kind; the
     * renderer refuses both violations. Bounded at 63 like every other subject
     * reference (SubjectDeclaration.name, VisibilityBinding.subject,
     * EventBinding.set_subject), so a name legal to DECLARE is always legal to
     * reference here. A NAME rather than a registry index deliberately: the
     * renderer's registry order is a decode-time implementation detail that a
     * dropped or reordered declaration silently shifts, so an index would
     * resolve to the WRONG subject and send a plausible wrong value, while an
     * unresolvable name fails loud at load.
     * ```
     *
     * `string subject = 5 [(.buf.validate.field) = { ... }`
     */
    public fun clearSubject() {
      _builder.clearSubject()
    }

    /**
     * `.ui.PatchEncoding encoding = 6 [(.buf.validate.field) = { ... }`
     */
    public var encoding: ui.UiAst.PatchEncoding
      @JvmName("getEncoding")
      get() = _builder.encoding
      @JvmName("setEncoding")
      set(value) {
        _builder.encoding = value
      }
    public var encodingValue: kotlin.Int
      @JvmName("getEncodingValue")
      get() = _builder.encodingValue
      @JvmName("setEncodingValue")
      set(value) {
        _builder.encodingValue = value
      }
    /**
     * `.ui.PatchEncoding encoding = 6 [(.buf.validate.field) = { ... }`
     */
    public fun clearEncoding() {
      _builder.clearEncoding()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun ui.UiAst.FieldPatch.copy(block: `ui`.FieldPatchKt.Dsl.() -> kotlin.Unit): ui.UiAst.FieldPatch =
  `ui`.FieldPatchKt.Dsl._create(this.toBuilder()).apply { block() }._build()

