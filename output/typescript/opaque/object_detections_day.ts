// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v5.29.2
// source: opaque/object_detections_day.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import {
  DetectionConfig,
  DetectionFrameMeta,
  DetectionStatus,
  detectionStatusFromJSON,
  detectionStatusToJSON,
  ObjectDetection,
} from "./detection_common";

/**
 * Object detection results for DAY camera channel.
 * Injected by cv-gateway into JonGUIState.opaque_payloads at inference rate (~30fps).
 * Errors logged to stderr, not embedded in proto.
 *
 * UUID: 019c40f6-825c-7f4c-8284-ddad4375ed9b
 */
export interface ObjectDetectionsDay {
  /** Inference status (required, must be valid enum value) */
  status: DetectionStatus;
  /** Detected objects (up to 256) */
  detections: ObjectDetection[];
  /** End-to-end inference latency (nanoseconds) */
  latencyNs: Long;
  /** Frame metadata for correlation */
  frame:
    | DetectionFrameMeta
    | undefined;
  /** Inference configuration used */
  config:
    | DetectionConfig
    | undefined;
  /** Correlation timestamp (CLOCK_MONOTONIC, microseconds) - matches CvMeta pattern */
  captureMonotonicUs: Long;
}

function createBaseObjectDetectionsDay(): ObjectDetectionsDay {
  return {
    status: 0,
    detections: [],
    latencyNs: Long.UZERO,
    frame: undefined,
    config: undefined,
    captureMonotonicUs: Long.UZERO,
  };
}

export const ObjectDetectionsDay: MessageFns<ObjectDetectionsDay> = {
  encode(message: ObjectDetectionsDay, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    for (const v of message.detections) {
      ObjectDetection.encode(v!, writer.uint32(18).fork()).join();
    }
    if (!message.latencyNs.equals(Long.UZERO)) {
      writer.uint32(24).uint64(message.latencyNs.toString());
    }
    if (message.frame !== undefined) {
      DetectionFrameMeta.encode(message.frame, writer.uint32(34).fork()).join();
    }
    if (message.config !== undefined) {
      DetectionConfig.encode(message.config, writer.uint32(42).fork()).join();
    }
    if (!message.captureMonotonicUs.equals(Long.UZERO)) {
      writer.uint32(48).uint64(message.captureMonotonicUs.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectDetectionsDay {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectDetectionsDay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.detections.push(ObjectDetection.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.latencyNs = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.frame = DetectionFrameMeta.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.config = DetectionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.captureMonotonicUs = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectDetectionsDay {
    return {
      status: isSet(object.status) ? detectionStatusFromJSON(object.status) : 0,
      detections: globalThis.Array.isArray(object?.detections)
        ? object.detections.map((e: any) => ObjectDetection.fromJSON(e))
        : [],
      latencyNs: isSet(object.latencyNs)
        ? Long.fromValue(object.latencyNs)
        : isSet(object.latency_ns)
        ? Long.fromValue(object.latency_ns)
        : Long.UZERO,
      frame: isSet(object.frame) ? DetectionFrameMeta.fromJSON(object.frame) : undefined,
      config: isSet(object.config) ? DetectionConfig.fromJSON(object.config) : undefined,
      captureMonotonicUs: isSet(object.captureMonotonicUs)
        ? Long.fromValue(object.captureMonotonicUs)
        : isSet(object.capture_monotonic_us)
        ? Long.fromValue(object.capture_monotonic_us)
        : Long.UZERO,
    };
  },

  toJSON(message: ObjectDetectionsDay): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = detectionStatusToJSON(message.status);
    }
    if (message.detections?.length) {
      obj.detections = message.detections.map((e) => ObjectDetection.toJSON(e));
    }
    if (!message.latencyNs.equals(Long.UZERO)) {
      obj.latencyNs = (message.latencyNs || Long.UZERO).toString();
    }
    if (message.frame !== undefined) {
      obj.frame = DetectionFrameMeta.toJSON(message.frame);
    }
    if (message.config !== undefined) {
      obj.config = DetectionConfig.toJSON(message.config);
    }
    if (!message.captureMonotonicUs.equals(Long.UZERO)) {
      obj.captureMonotonicUs = (message.captureMonotonicUs || Long.UZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectDetectionsDay>, I>>(base?: I): ObjectDetectionsDay {
    return ObjectDetectionsDay.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectDetectionsDay>, I>>(object: I): ObjectDetectionsDay {
    const message = createBaseObjectDetectionsDay();
    message.status = object.status ?? 0;
    message.detections = object.detections?.map((e) => ObjectDetection.fromPartial(e)) || [];
    message.latencyNs = (object.latencyNs !== undefined && object.latencyNs !== null)
      ? Long.fromValue(object.latencyNs)
      : Long.UZERO;
    message.frame = (object.frame !== undefined && object.frame !== null)
      ? DetectionFrameMeta.fromPartial(object.frame)
      : undefined;
    message.config = (object.config !== undefined && object.config !== null)
      ? DetectionConfig.fromPartial(object.config)
      : undefined;
    message.captureMonotonicUs = (object.captureMonotonicUs !== undefined && object.captureMonotonicUs !== null)
      ? Long.fromValue(object.captureMonotonicUs)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
