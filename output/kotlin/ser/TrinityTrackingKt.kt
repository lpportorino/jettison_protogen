// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: opaque/trinity_tracking.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package ser;

@kotlin.jvm.JvmName("-initializetrinityTracking")
public inline fun trinityTracking(block: ser.TrinityTrackingKt.Dsl.() -> kotlin.Unit): ser.TrinityTrackingOuterClass.TrinityTracking =
  ser.TrinityTrackingKt.Dsl._create(ser.TrinityTrackingOuterClass.TrinityTracking.newBuilder()).apply { block() }._build()
/**
 * ```
 * Full-precision pose of the Ring-Trinity golden fiducial board.
 * Injected into JonGUIState.opaque_payloads by the trinity tracker at track rate.
 *
 * WHY DOUBLES AND NOT NDC: the SAM tracking payloads carry NDC in [-1,1] because a
 * bounding box is a screen artifact. This payload carries a METRIC POSE — the board's
 * position in metres and its orientation as a unit quaternion — because it is the
 * ground-truth judge other measurements are scored against. Quantising it to screen
 * coordinates would destroy the precision the board exists to provide.
 *
 * UUID: 019facc1-62a1-74a8-9a75-2c195213c3d8
 * ```
 *
 * Protobuf type `ser.TrinityTracking`
 */
public object TrinityTrackingKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: ser.TrinityTrackingOuterClass.TrinityTracking.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: ser.TrinityTrackingOuterClass.TrinityTracking.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): ser.TrinityTrackingOuterClass.TrinityTracking = _builder.build()

    /**
     * ```
     * ─── identity of the physical target ────────────────────────────────────────
     * WHICH BOARD this pose refers to. This is DATA, not the payload's schema
     * version — JonOpaquePayload.version already carries that, and the two move
     * independently: a schema change does not reprint the board, and a new board
     * revision does not change this message's shape. Conflating them makes a
     * re-print indistinguishable from a wire-format bump.
     * ```
     *
     * `.ser.TrinityBoardVersion board_version = 1 [(.buf.validate.field) = { ... }`
     */
    public var boardVersion: ser.TrinityTrackingOuterClass.TrinityBoardVersion
      @JvmName("getBoardVersion")
      get() = _builder.boardVersion
      @JvmName("setBoardVersion")
      set(value) {
        _builder.boardVersion = value
      }
    /**
     * ```
     * ─── identity of the physical target ────────────────────────────────────────
     * WHICH BOARD this pose refers to. This is DATA, not the payload's schema
     * version — JonOpaquePayload.version already carries that, and the two move
     * independently: a schema change does not reprint the board, and a new board
     * revision does not change this message's shape. Conflating them makes a
     * re-print indistinguishable from a wire-format bump.
     * ```
     *
     * `.ser.TrinityBoardVersion board_version = 1 [(.buf.validate.field) = { ... }`
     */
    public fun clearBoardVersion() {
      _builder.clearBoardVersion()
    }
    /**
     * ```
     * ─── identity of the physical target ────────────────────────────────────────
     * WHICH BOARD this pose refers to. This is DATA, not the payload's schema
     * version — JonOpaquePayload.version already carries that, and the two move
     * independently: a schema change does not reprint the board, and a new board
     * revision does not change this message's shape. Conflating them makes a
     * re-print indistinguishable from a wire-format bump.
     * ```
     *
     * `.ser.TrinityBoardVersion board_version = 1 [(.buf.validate.field) = { ... }`
     * @return Whether the boardVersion field is set.
     */
    public fun hasBoardVersion(): kotlin.Boolean {
      return _builder.hasBoardVersion()
    }

    public val TrinityTrackingKt.Dsl.boardVersionOrNull: ser.TrinityTrackingOuterClass.TrinityBoardVersion?
      get() = _builder.boardVersionOrNull

    /**
     * ```
     * ─── when ───────────────────────────────────────────────────────────────────
     * CLOCK_BOOTTIME nanoseconds of the frame this pose was computed from — the
     * same clock as a cv-dump's capture_time_ns and manifest t0_boot_ns/t1_boot_ns,
     * so a pose joins against telemetry without a clock conversion.
     * ```
     *
     * `uint64 capture_time_ns = 2 [(.buf.validate.field) = { ... }`
     */
    public var captureTimeNs: kotlin.Long
      @JvmName("getCaptureTimeNs")
      get() = _builder.captureTimeNs
      @JvmName("setCaptureTimeNs")
      set(value) {
        _builder.captureTimeNs = value
      }
    /**
     * ```
     * ─── when ───────────────────────────────────────────────────────────────────
     * CLOCK_BOOTTIME nanoseconds of the frame this pose was computed from — the
     * same clock as a cv-dump's capture_time_ns and manifest t0_boot_ns/t1_boot_ns,
     * so a pose joins against telemetry without a clock conversion.
     * ```
     *
     * `uint64 capture_time_ns = 2 [(.buf.validate.field) = { ... }`
     */
    public fun clearCaptureTimeNs() {
      _builder.clearCaptureTimeNs()
    }

    /**
     * `.ser.TrinityTrackingStatus status = 3 [(.buf.validate.field) = { ... }`
     */
    public var status: ser.TrinityTrackingOuterClass.TrinityTrackingStatus
      @JvmName("getStatus")
      get() = _builder.status
      @JvmName("setStatus")
      set(value) {
        _builder.status = value
      }
    public var statusValue: kotlin.Int
      @JvmName("getStatusValue")
      get() = _builder.statusValue
      @JvmName("setStatusValue")
      set(value) {
        _builder.statusValue = value
      }
    /**
     * `.ser.TrinityTrackingStatus status = 3 [(.buf.validate.field) = { ... }`
     */
    public fun clearStatus() {
      _builder.clearStatus()
    }

    /**
     * ```
     * ─── position: board origin in CAMERA frame, metres ─────────────────────────
     * Right-handed, +X right, +Y down, +Z forward along the optical axis.
     *
     * PRECISION, MEASURED RATHER THAN PROMISED. `double` is not the limit — at 10 m
     * it resolves to ~1.8e-12 mm, over-provisioned for millimetres by ~12 orders of
     * magnitude. The OPTICS are the limit, and they are ANISOTROPIC:
     *
     * LATERAL (x, y) = sigma_px * range / focal_px. At 0.4 px centre localisation:
     * day  (focal_px 3517):  1.1 mm @ 10 m,   5.7 mm @ 50 m
     * heat (focal_px 1320):  3.0 mm @ 10 m,  15.2 mm @ 50 m
     *
     * RANGE (z) from apparent SIZE is far worse, because dZ/Z = dS/S and the board
     * subtends few pixels: 27 mm @ 10 m day, 676 mm @ 50 m day. It degrades with
     * the SQUARE of range, since the extent shrinks as the error stays fixed.
     *
     * So mm-level is achievable LATERALLY on day at short range, and is NOT
     * achievable in RANGE from board extent alone at any useful distance. That
     * asymmetry is why sigma_range_m is separate from sigma_position_m, and why
     * range_source exists.
     * ```
     *
     * `double position_x_m = 4;`
     */
    public var positionXM: kotlin.Double
      @JvmName("getPositionXM")
      get() = _builder.positionXM
      @JvmName("setPositionXM")
      set(value) {
        _builder.positionXM = value
      }
    /**
     * ```
     * ─── position: board origin in CAMERA frame, metres ─────────────────────────
     * Right-handed, +X right, +Y down, +Z forward along the optical axis.
     *
     * PRECISION, MEASURED RATHER THAN PROMISED. `double` is not the limit — at 10 m
     * it resolves to ~1.8e-12 mm, over-provisioned for millimetres by ~12 orders of
     * magnitude. The OPTICS are the limit, and they are ANISOTROPIC:
     *
     * LATERAL (x, y) = sigma_px * range / focal_px. At 0.4 px centre localisation:
     * day  (focal_px 3517):  1.1 mm @ 10 m,   5.7 mm @ 50 m
     * heat (focal_px 1320):  3.0 mm @ 10 m,  15.2 mm @ 50 m
     *
     * RANGE (z) from apparent SIZE is far worse, because dZ/Z = dS/S and the board
     * subtends few pixels: 27 mm @ 10 m day, 676 mm @ 50 m day. It degrades with
     * the SQUARE of range, since the extent shrinks as the error stays fixed.
     *
     * So mm-level is achievable LATERALLY on day at short range, and is NOT
     * achievable in RANGE from board extent alone at any useful distance. That
     * asymmetry is why sigma_range_m is separate from sigma_position_m, and why
     * range_source exists.
     * ```
     *
     * `double position_x_m = 4;`
     */
    public fun clearPositionXM() {
      _builder.clearPositionXM()
    }

    /**
     * `double position_y_m = 5;`
     */
    public var positionYM: kotlin.Double
      @JvmName("getPositionYM")
      get() = _builder.positionYM
      @JvmName("setPositionYM")
      set(value) {
        _builder.positionYM = value
      }
    /**
     * `double position_y_m = 5;`
     */
    public fun clearPositionYM() {
      _builder.clearPositionYM()
    }

    /**
     * `double position_z_m = 6 [(.buf.validate.field) = { ... }`
     */
    public var positionZM: kotlin.Double
      @JvmName("getPositionZM")
      get() = _builder.positionZM
      @JvmName("setPositionZM")
      set(value) {
        _builder.positionZM = value
      }
    /**
     * `double position_z_m = 6 [(.buf.validate.field) = { ... }`
     */
    public fun clearPositionZM() {
      _builder.clearPositionZM()
    }

    /**
     * ```
     * ─── orientation: unit quaternion, board -> camera ──────────────────────────
     * A QUATERNION AND NOT EULER ANGLES, deliberately: Euler loses a degree of
     * freedom at +/-90 degrees pitch (gimbal lock), and the board is routinely
     * viewed near-edge-on where that singularity is reachable. A consumer wanting
     * Euler converts; a producer emitting Euler cannot un-lose the DoF.
     * Normalised: |q| = 1 within 1e-9.
     * ```
     *
     * `double quat_w = 7 [(.buf.validate.field) = { ... }`
     */
    public var quatW: kotlin.Double
      @JvmName("getQuatW")
      get() = _builder.quatW
      @JvmName("setQuatW")
      set(value) {
        _builder.quatW = value
      }
    /**
     * ```
     * ─── orientation: unit quaternion, board -> camera ──────────────────────────
     * A QUATERNION AND NOT EULER ANGLES, deliberately: Euler loses a degree of
     * freedom at +/-90 degrees pitch (gimbal lock), and the board is routinely
     * viewed near-edge-on where that singularity is reachable. A consumer wanting
     * Euler converts; a producer emitting Euler cannot un-lose the DoF.
     * Normalised: |q| = 1 within 1e-9.
     * ```
     *
     * `double quat_w = 7 [(.buf.validate.field) = { ... }`
     */
    public fun clearQuatW() {
      _builder.clearQuatW()
    }

    /**
     * `double quat_x = 8 [(.buf.validate.field) = { ... }`
     */
    public var quatX: kotlin.Double
      @JvmName("getQuatX")
      get() = _builder.quatX
      @JvmName("setQuatX")
      set(value) {
        _builder.quatX = value
      }
    /**
     * `double quat_x = 8 [(.buf.validate.field) = { ... }`
     */
    public fun clearQuatX() {
      _builder.clearQuatX()
    }

    /**
     * `double quat_y = 9 [(.buf.validate.field) = { ... }`
     */
    public var quatY: kotlin.Double
      @JvmName("getQuatY")
      get() = _builder.quatY
      @JvmName("setQuatY")
      set(value) {
        _builder.quatY = value
      }
    /**
     * `double quat_y = 9 [(.buf.validate.field) = { ... }`
     */
    public fun clearQuatY() {
      _builder.clearQuatY()
    }

    /**
     * `double quat_z = 10 [(.buf.validate.field) = { ... }`
     */
    public var quatZ: kotlin.Double
      @JvmName("getQuatZ")
      get() = _builder.quatZ
      @JvmName("setQuatZ")
      set(value) {
        _builder.quatZ = value
      }
    /**
     * `double quat_z = 10 [(.buf.validate.field) = { ... }`
     */
    public fun clearQuatZ() {
      _builder.clearQuatZ()
    }

    /**
     * ```
     * ─── per-axis confidence, because one scalar cannot say what is true ────────
     * ROBUSTNESS AND PRECISION ARE DIFFERENT ORDERINGS HERE, and a consumer needs
     * both. By ROBUSTNESS at far range: lateral and range stay OBSERVABLE while
     * full 3-DoF orientation does not (the near-affine two-fold ambiguity). By
     * PRECISION: lateral is best, range is ~20x worse, orientation worst. A single
     * confidence number would have to lie about at least one axis, so consumers
     * gate on the axis they actually use.
     * 1-sigma uncertainties; negative means "not estimated".
     * ```
     *
     * `double sigma_position_m = 11;`
     */
    public var sigmaPositionM: kotlin.Double
      @JvmName("getSigmaPositionM")
      get() = _builder.sigmaPositionM
      @JvmName("setSigmaPositionM")
      set(value) {
        _builder.sigmaPositionM = value
      }
    /**
     * ```
     * ─── per-axis confidence, because one scalar cannot say what is true ────────
     * ROBUSTNESS AND PRECISION ARE DIFFERENT ORDERINGS HERE, and a consumer needs
     * both. By ROBUSTNESS at far range: lateral and range stay OBSERVABLE while
     * full 3-DoF orientation does not (the near-affine two-fold ambiguity). By
     * PRECISION: lateral is best, range is ~20x worse, orientation worst. A single
     * confidence number would have to lie about at least one axis, so consumers
     * gate on the axis they actually use.
     * 1-sigma uncertainties; negative means "not estimated".
     * ```
     *
     * `double sigma_position_m = 11;`
     */
    public fun clearSigmaPositionM() {
      _builder.clearSigmaPositionM()
    }

    /**
     * ```
     * 1-sigma on Z alone. When range_source is
     * ```
     *
     * `double sigma_range_m = 12;`
     */
    public var sigmaRangeM: kotlin.Double
      @JvmName("getSigmaRangeM")
      get() = _builder.sigmaRangeM
      @JvmName("setSigmaRangeM")
      set(value) {
        _builder.sigmaRangeM = value
      }
    /**
     * ```
     * 1-sigma on Z alone. When range_source is
     * ```
     *
     * `double sigma_range_m = 12;`
     */
    public fun clearSigmaRangeM() {
      _builder.clearSigmaRangeM()
    }

    /**
     * ```
     * BOARD_EXTENT this is MUCH LARGER than
     * sigma_position_m (27 mm vs 1.1 mm at 10 m
     * day); with LRF it can be smaller. Never
     * assume the relation — read range_source.
     * ```
     *
     * `double sigma_orientation_mrad = 13;`
     */
    public var sigmaOrientationMrad: kotlin.Double
      @JvmName("getSigmaOrientationMrad")
      get() = _builder.sigmaOrientationMrad
      @JvmName("setSigmaOrientationMrad")
      set(value) {
        _builder.sigmaOrientationMrad = value
      }
    /**
     * ```
     * BOARD_EXTENT this is MUCH LARGER than
     * sigma_position_m (27 mm vs 1.1 mm at 10 m
     * day); with LRF it can be smaller. Never
     * assume the relation — read range_source.
     * ```
     *
     * `double sigma_orientation_mrad = 13;`
     */
    public fun clearSigmaOrientationMrad() {
      _builder.clearSigmaOrientationMrad()
    }

    /**
     * ```
     * ─── the IPPE two-fold ambiguity, surfaced rather than hidden ───────────────
     * A planar target under near-affine projection admits TWO poses that
     * reprojection error cannot separate. A payload that emits only the chosen one
     * is silently wrong about half the time at range. The alternate is carried so a
     * consumer can see the fork and apply its own prior.
     * ```
     *
     * `bool ambiguity_resolved = 14;`
     */
    public var ambiguityResolved: kotlin.Boolean
      @JvmName("getAmbiguityResolved")
      get() = _builder.ambiguityResolved
      @JvmName("setAmbiguityResolved")
      set(value) {
        _builder.ambiguityResolved = value
      }
    /**
     * ```
     * ─── the IPPE two-fold ambiguity, surfaced rather than hidden ───────────────
     * A planar target under near-affine projection admits TWO poses that
     * reprojection error cannot separate. A payload that emits only the chosen one
     * is silently wrong about half the time at range. The alternate is carried so a
     * consumer can see the fork and apply its own prior.
     * ```
     *
     * `bool ambiguity_resolved = 14;`
     */
    public fun clearAmbiguityResolved() {
      _builder.clearAmbiguityResolved()
    }

    /**
     * ```
     * unset when only one solution exists
     * ```
     *
     * `.ser.TrinityAltPose alternate = 15;`
     */
    public var alternate: ser.TrinityTrackingOuterClass.TrinityAltPose
      @JvmName("getAlternate")
      get() = _builder.alternate
      @JvmName("setAlternate")
      set(value) {
        _builder.alternate = value
      }
    /**
     * ```
     * unset when only one solution exists
     * ```
     *
     * `.ser.TrinityAltPose alternate = 15;`
     */
    public fun clearAlternate() {
      _builder.clearAlternate()
    }
    /**
     * ```
     * unset when only one solution exists
     * ```
     *
     * `.ser.TrinityAltPose alternate = 15;`
     * @return Whether the alternate field is set.
     */
    public fun hasAlternate(): kotlin.Boolean {
      return _builder.hasAlternate()
    }

    public val TrinityTrackingKt.Dsl.alternateOrNull: ser.TrinityTrackingOuterClass.TrinityAltPose?
      get() = _builder.alternateOrNull

    /**
     * ```
     * ─── where range came from ──────────────────────────────────────────────────
     * Range from board extent is the weakest axis (see position_z_m). The platform
     * carries an LRF whose distance is direct and roughly range-independent, so a
     * fused pose can be mm-class in Z where a vision-only pose cannot. A consumer
     * MUST be able to tell which it received: the two differ by more than an order
     * of magnitude at 50 m, and they look identical on the wire.
     * ```
     *
     * `.ser.TrinityRangeSource range_source = 19 [(.buf.validate.field) = { ... }`
     */
    public var rangeSource: ser.TrinityTrackingOuterClass.TrinityRangeSource
      @JvmName("getRangeSource")
      get() = _builder.rangeSource
      @JvmName("setRangeSource")
      set(value) {
        _builder.rangeSource = value
      }
    public var rangeSourceValue: kotlin.Int
      @JvmName("getRangeSourceValue")
      get() = _builder.rangeSourceValue
      @JvmName("setRangeSourceValue")
      set(value) {
        _builder.rangeSourceValue = value
      }
    /**
     * ```
     * ─── where range came from ──────────────────────────────────────────────────
     * Range from board extent is the weakest axis (see position_z_m). The platform
     * carries an LRF whose distance is direct and roughly range-independent, so a
     * fused pose can be mm-class in Z where a vision-only pose cannot. A consumer
     * MUST be able to tell which it received: the two differ by more than an order
     * of magnitude at 50 m, and they look identical on the wire.
     * ```
     *
     * `.ser.TrinityRangeSource range_source = 19 [(.buf.validate.field) = { ... }`
     */
    public fun clearRangeSource() {
      _builder.clearRangeSource()
    }

    /**
     * ```
     * ─── observability, for judging the pose rather than trusting it ────────────
     * ```
     *
     * `uint32 anchors_seen = 16 [(.buf.validate.field) = { ... }`
     */
    public var anchorsSeen: kotlin.Int
      @JvmName("getAnchorsSeen")
      get() = _builder.anchorsSeen
      @JvmName("setAnchorsSeen")
      set(value) {
        _builder.anchorsSeen = value
      }
    /**
     * ```
     * ─── observability, for judging the pose rather than trusting it ────────────
     * ```
     *
     * `uint32 anchors_seen = 16 [(.buf.validate.field) = { ... }`
     */
    public fun clearAnchorsSeen() {
      _builder.clearAnchorsSeen()
    }

    /**
     * `double board_extent_px = 17 [(.buf.validate.field) = { ... }`
     */
    public var boardExtentPx: kotlin.Double
      @JvmName("getBoardExtentPx")
      get() = _builder.boardExtentPx
      @JvmName("setBoardExtentPx")
      set(value) {
        _builder.boardExtentPx = value
      }
    /**
     * `double board_extent_px = 17 [(.buf.validate.field) = { ... }`
     */
    public fun clearBoardExtentPx() {
      _builder.clearBoardExtentPx()
    }

    /**
     * `double reprojection_rms_px = 18 [(.buf.validate.field) = { ... }`
     */
    public var reprojectionRmsPx: kotlin.Double
      @JvmName("getReprojectionRmsPx")
      get() = _builder.reprojectionRmsPx
      @JvmName("setReprojectionRmsPx")
      set(value) {
        _builder.reprojectionRmsPx = value
      }
    /**
     * `double reprojection_rms_px = 18 [(.buf.validate.field) = { ... }`
     */
    public fun clearReprojectionRmsPx() {
      _builder.clearReprojectionRmsPx()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun ser.TrinityTrackingOuterClass.TrinityTracking.copy(block: `ser`.TrinityTrackingKt.Dsl.() -> kotlin.Unit): ser.TrinityTrackingOuterClass.TrinityTracking =
  `ser`.TrinityTrackingKt.Dsl._create(this.toBuilder()).apply { block() }._build()

public val ser.TrinityTrackingOuterClass.TrinityTrackingOrBuilder.boardVersionOrNull: ser.TrinityTrackingOuterClass.TrinityBoardVersion?
  get() = if (hasBoardVersion()) getBoardVersion() else null

public val ser.TrinityTrackingOuterClass.TrinityTrackingOrBuilder.alternateOrNull: ser.TrinityTrackingOuterClass.TrinityAltPose?
  get() = if (hasAlternate()) getAlternate() else null

