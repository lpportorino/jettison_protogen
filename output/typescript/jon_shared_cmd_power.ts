// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v5.29.2
// source: jon_shared_cmd_power.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** Power module control commands root */
export interface Root {
  setChannel?: SetChannel | undefined;
  setAll?: SetAll | undefined;
  setAlertThreshold?: SetAlertThreshold | undefined;
}

/** Set power state for a single channel */
export interface SetChannel {
  channel: number;
  powerOn: boolean;
}

/** Set power state for all channels */
export interface SetAll {
  powerOn: boolean;
}

/** Set overcurrent alert threshold for a channel */
export interface SetAlertThreshold {
  channel: number;
  /** Threshold in milliamps */
  thresholdMa: number;
}

function createBaseRoot(): Root {
  return { setChannel: undefined, setAll: undefined, setAlertThreshold: undefined };
}

export const Root: MessageFns<Root> = {
  encode(message: Root, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setChannel !== undefined) {
      SetChannel.encode(message.setChannel, writer.uint32(10).fork()).join();
    }
    if (message.setAll !== undefined) {
      SetAll.encode(message.setAll, writer.uint32(18).fork()).join();
    }
    if (message.setAlertThreshold !== undefined) {
      SetAlertThreshold.encode(message.setAlertThreshold, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Root {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setChannel = SetChannel.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setAll = SetAll.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.setAlertThreshold = SetAlertThreshold.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Root {
    return {
      setChannel: isSet(object.setChannel) ? SetChannel.fromJSON(object.setChannel) : undefined,
      setAll: isSet(object.setAll) ? SetAll.fromJSON(object.setAll) : undefined,
      setAlertThreshold: isSet(object.setAlertThreshold)
        ? SetAlertThreshold.fromJSON(object.setAlertThreshold)
        : undefined,
    };
  },

  toJSON(message: Root): unknown {
    const obj: any = {};
    if (message.setChannel !== undefined) {
      obj.setChannel = SetChannel.toJSON(message.setChannel);
    }
    if (message.setAll !== undefined) {
      obj.setAll = SetAll.toJSON(message.setAll);
    }
    if (message.setAlertThreshold !== undefined) {
      obj.setAlertThreshold = SetAlertThreshold.toJSON(message.setAlertThreshold);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Root>, I>>(base?: I): Root {
    return Root.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Root>, I>>(object: I): Root {
    const message = createBaseRoot();
    message.setChannel = (object.setChannel !== undefined && object.setChannel !== null)
      ? SetChannel.fromPartial(object.setChannel)
      : undefined;
    message.setAll = (object.setAll !== undefined && object.setAll !== null)
      ? SetAll.fromPartial(object.setAll)
      : undefined;
    message.setAlertThreshold = (object.setAlertThreshold !== undefined && object.setAlertThreshold !== null)
      ? SetAlertThreshold.fromPartial(object.setAlertThreshold)
      : undefined;
    return message;
  },
};

function createBaseSetChannel(): SetChannel {
  return { channel: 0, powerOn: false };
}

export const SetChannel: MessageFns<SetChannel> = {
  encode(message: SetChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== 0) {
      writer.uint32(8).uint32(message.channel);
    }
    if (message.powerOn !== false) {
      writer.uint32(16).bool(message.powerOn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetChannel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.powerOn = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetChannel {
    return {
      channel: isSet(object.channel) ? globalThis.Number(object.channel) : 0,
      powerOn: isSet(object.powerOn) ? globalThis.Boolean(object.powerOn) : false,
    };
  },

  toJSON(message: SetChannel): unknown {
    const obj: any = {};
    if (message.channel !== 0) {
      obj.channel = Math.round(message.channel);
    }
    if (message.powerOn !== false) {
      obj.powerOn = message.powerOn;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetChannel>, I>>(base?: I): SetChannel {
    return SetChannel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetChannel>, I>>(object: I): SetChannel {
    const message = createBaseSetChannel();
    message.channel = object.channel ?? 0;
    message.powerOn = object.powerOn ?? false;
    return message;
  },
};

function createBaseSetAll(): SetAll {
  return { powerOn: false };
}

export const SetAll: MessageFns<SetAll> = {
  encode(message: SetAll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.powerOn !== false) {
      writer.uint32(8).bool(message.powerOn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAll();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.powerOn = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAll {
    return { powerOn: isSet(object.powerOn) ? globalThis.Boolean(object.powerOn) : false };
  },

  toJSON(message: SetAll): unknown {
    const obj: any = {};
    if (message.powerOn !== false) {
      obj.powerOn = message.powerOn;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetAll>, I>>(base?: I): SetAll {
    return SetAll.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetAll>, I>>(object: I): SetAll {
    const message = createBaseSetAll();
    message.powerOn = object.powerOn ?? false;
    return message;
  },
};

function createBaseSetAlertThreshold(): SetAlertThreshold {
  return { channel: 0, thresholdMa: 0 };
}

export const SetAlertThreshold: MessageFns<SetAlertThreshold> = {
  encode(message: SetAlertThreshold, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== 0) {
      writer.uint32(8).uint32(message.channel);
    }
    if (message.thresholdMa !== 0) {
      writer.uint32(16).uint32(message.thresholdMa);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAlertThreshold {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAlertThreshold();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.thresholdMa = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAlertThreshold {
    return {
      channel: isSet(object.channel) ? globalThis.Number(object.channel) : 0,
      thresholdMa: isSet(object.thresholdMa) ? globalThis.Number(object.thresholdMa) : 0,
    };
  },

  toJSON(message: SetAlertThreshold): unknown {
    const obj: any = {};
    if (message.channel !== 0) {
      obj.channel = Math.round(message.channel);
    }
    if (message.thresholdMa !== 0) {
      obj.thresholdMa = Math.round(message.thresholdMa);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetAlertThreshold>, I>>(base?: I): SetAlertThreshold {
    return SetAlertThreshold.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetAlertThreshold>, I>>(object: I): SetAlertThreshold {
    const message = createBaseSetAlertThreshold();
    message.channel = object.channel ?? 0;
    message.thresholdMa = object.thresholdMa ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
