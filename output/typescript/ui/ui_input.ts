// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.29.2
// source: ui/ui_input.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/**
 * Channel schema version registry — the canonical list of valid versions. The
 * wire field is a uint32 (range-validated {gte:1,lte:255}) so it fail-fast
 * rejects the proto3 default 0; this enum documents which values are valid.
 */
export enum InputSchemaVersion {
  INPUT_SCHEMA_VERSION_UNSPECIFIED = 0,
  INPUT_SCHEMA_VERSION_V1 = 1,
  UNRECOGNIZED = -1,
}

export function inputSchemaVersionFromJSON(object: any): InputSchemaVersion {
  switch (object) {
    case 0:
    case "INPUT_SCHEMA_VERSION_UNSPECIFIED":
      return InputSchemaVersion.INPUT_SCHEMA_VERSION_UNSPECIFIED;
    case 1:
    case "INPUT_SCHEMA_VERSION_V1":
      return InputSchemaVersion.INPUT_SCHEMA_VERSION_V1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InputSchemaVersion.UNRECOGNIZED;
  }
}

export function inputSchemaVersionToJSON(object: InputSchemaVersion): string {
  switch (object) {
    case InputSchemaVersion.INPUT_SCHEMA_VERSION_UNSPECIFIED:
      return "INPUT_SCHEMA_VERSION_UNSPECIFIED";
    case InputSchemaVersion.INPUT_SCHEMA_VERSION_V1:
      return "INPUT_SCHEMA_VERSION_V1";
    case InputSchemaVersion.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * W3C-style pointer lifecycle phase (host input device → WASM hit-test + FSM).
 * CANCEL = pointercancel / lostpointercapture (palm-reject / OS-takeover /
 * capture loss) — the FSM hard-aborts the pointer and emits NO terminal, so a
 * lost contact never fires a phantom pan-end / tap device command.
 */
export enum PointerPhase {
  POINTER_PHASE_UNSPECIFIED = 0,
  POINTER_PHASE_DOWN = 1,
  POINTER_PHASE_MOVE = 2,
  POINTER_PHASE_UP = 3,
  POINTER_PHASE_CANCEL = 4,
  UNRECOGNIZED = -1,
}

export function pointerPhaseFromJSON(object: any): PointerPhase {
  switch (object) {
    case 0:
    case "POINTER_PHASE_UNSPECIFIED":
      return PointerPhase.POINTER_PHASE_UNSPECIFIED;
    case 1:
    case "POINTER_PHASE_DOWN":
      return PointerPhase.POINTER_PHASE_DOWN;
    case 2:
    case "POINTER_PHASE_MOVE":
      return PointerPhase.POINTER_PHASE_MOVE;
    case 3:
    case "POINTER_PHASE_UP":
      return PointerPhase.POINTER_PHASE_UP;
    case 4:
    case "POINTER_PHASE_CANCEL":
      return PointerPhase.POINTER_PHASE_CANCEL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PointerPhase.UNRECOGNIZED;
  }
}

export function pointerPhaseToJSON(object: PointerPhase): string {
  switch (object) {
    case PointerPhase.POINTER_PHASE_UNSPECIFIED:
      return "POINTER_PHASE_UNSPECIFIED";
    case PointerPhase.POINTER_PHASE_DOWN:
      return "POINTER_PHASE_DOWN";
    case PointerPhase.POINTER_PHASE_MOVE:
      return "POINTER_PHASE_MOVE";
    case PointerPhase.POINTER_PHASE_UP:
      return "POINTER_PHASE_UP";
    case PointerPhase.POINTER_PHASE_CANCEL:
      return "POINTER_PHASE_CANCEL";
    case PointerPhase.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Pointing-device kind (W3C pointerType), for LVGL input semantics + the
 * hit-test path (mouse can hover without contact; touch cannot).
 */
export enum PointerKind {
  POINTER_KIND_UNSPECIFIED = 0,
  POINTER_KIND_MOUSE = 1,
  POINTER_KIND_TOUCH = 2,
  POINTER_KIND_PEN = 3,
  UNRECOGNIZED = -1,
}

export function pointerKindFromJSON(object: any): PointerKind {
  switch (object) {
    case 0:
    case "POINTER_KIND_UNSPECIFIED":
      return PointerKind.POINTER_KIND_UNSPECIFIED;
    case 1:
    case "POINTER_KIND_MOUSE":
      return PointerKind.POINTER_KIND_MOUSE;
    case 2:
    case "POINTER_KIND_TOUCH":
      return PointerKind.POINTER_KIND_TOUCH;
    case 3:
    case "POINTER_KIND_PEN":
      return PointerKind.POINTER_KIND_PEN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PointerKind.UNRECOGNIZED;
  }
}

export function pointerKindToJSON(object: PointerKind): string {
  switch (object) {
    case PointerKind.POINTER_KIND_UNSPECIFIED:
      return "POINTER_KIND_UNSPECIFIED";
    case PointerKind.POINTER_KIND_MOUSE:
      return "POINTER_KIND_MOUSE";
    case PointerKind.POINTER_KIND_TOUCH:
      return "POINTER_KIND_TOUCH";
    case PointerKind.POINTER_KIND_PEN:
      return "POINTER_KIND_PEN";
    case PointerKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** OS theme the host reports for WASM restyle. */
export enum ThemeMode {
  THEME_MODE_UNSPECIFIED = 0,
  THEME_MODE_LIGHT = 1,
  THEME_MODE_DARK = 2,
  UNRECOGNIZED = -1,
}

export function themeModeFromJSON(object: any): ThemeMode {
  switch (object) {
    case 0:
    case "THEME_MODE_UNSPECIFIED":
      return ThemeMode.THEME_MODE_UNSPECIFIED;
    case 1:
    case "THEME_MODE_LIGHT":
      return ThemeMode.THEME_MODE_LIGHT;
    case 2:
    case "THEME_MODE_DARK":
      return ThemeMode.THEME_MODE_DARK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ThemeMode.UNRECOGNIZED;
  }
}

export function themeModeToJSON(object: ThemeMode): string {
  switch (object) {
    case ThemeMode.THEME_MODE_UNSPECIFIED:
      return "THEME_MODE_UNSPECIFIED";
    case ThemeMode.THEME_MODE_LIGHT:
      return "THEME_MODE_LIGHT";
    case ThemeMode.THEME_MODE_DARK:
      return "THEME_MODE_DARK";
    case ThemeMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Cursor the WASM requests the host render over the hovered region. */
export enum CursorType {
  CURSOR_TYPE_UNSPECIFIED = 0,
  CURSOR_TYPE_DEFAULT = 1,
  CURSOR_TYPE_POINTER = 2,
  CURSOR_TYPE_TEXT = 3,
  CURSOR_TYPE_GRAB = 4,
  CURSOR_TYPE_RESIZE = 5,
  CURSOR_TYPE_NOT_ALLOWED = 6,
  UNRECOGNIZED = -1,
}

export function cursorTypeFromJSON(object: any): CursorType {
  switch (object) {
    case 0:
    case "CURSOR_TYPE_UNSPECIFIED":
      return CursorType.CURSOR_TYPE_UNSPECIFIED;
    case 1:
    case "CURSOR_TYPE_DEFAULT":
      return CursorType.CURSOR_TYPE_DEFAULT;
    case 2:
    case "CURSOR_TYPE_POINTER":
      return CursorType.CURSOR_TYPE_POINTER;
    case 3:
    case "CURSOR_TYPE_TEXT":
      return CursorType.CURSOR_TYPE_TEXT;
    case 4:
    case "CURSOR_TYPE_GRAB":
      return CursorType.CURSOR_TYPE_GRAB;
    case 5:
    case "CURSOR_TYPE_RESIZE":
      return CursorType.CURSOR_TYPE_RESIZE;
    case 6:
    case "CURSOR_TYPE_NOT_ALLOWED":
      return CursorType.CURSOR_TYPE_NOT_ALLOWED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CursorType.UNRECOGNIZED;
  }
}

export function cursorTypeToJSON(object: CursorType): string {
  switch (object) {
    case CursorType.CURSOR_TYPE_UNSPECIFIED:
      return "CURSOR_TYPE_UNSPECIFIED";
    case CursorType.CURSOR_TYPE_DEFAULT:
      return "CURSOR_TYPE_DEFAULT";
    case CursorType.CURSOR_TYPE_POINTER:
      return "CURSOR_TYPE_POINTER";
    case CursorType.CURSOR_TYPE_TEXT:
      return "CURSOR_TYPE_TEXT";
    case CursorType.CURSOR_TYPE_GRAB:
      return "CURSOR_TYPE_GRAB";
    case CursorType.CURSOR_TYPE_RESIZE:
      return "CURSOR_TYPE_RESIZE";
    case CursorType.CURSOR_TYPE_NOT_ALLOWED:
      return "CURSOR_TYPE_NOT_ALLOWED";
    case CursorType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A bounded adaptation of the W3C Pointer Events API (mouse + touch + pen
 * unified). The host forwards every pointer event; the WASM accumulates them
 * into its fixed pointer-state table by `pointer_id` and runs the gesture FSM.
 * The WASM SELF-VALIDATES at the decode boundary (nanopb strips buf.validate):
 * reject phase/kind = 0, reject event_time = 0, clamp NDC, find-slot-or-drop on
 * pointer_id, clamp non-positive FSM time deltas.
 */
export interface PointerEvent {
  phase: PointerPhase;
  kind: PointerKind;
  /** W3C pointerId — the multi-pointer FSM key (pinch keys on ≥2) */
  pointerId: number;
  /** NDC, +x right */
  x: number;
  /** NDC, +y UP */
  y: number;
  /** W3C event.timeStamp, ms — the FSM clock (EVENT time, NOT the render tick) */
  eventTime: Long;
}

/**
 * OS lifecycle the host pushes for WASM restyle / pause. focused/visible = false
 * also doubles as the whole-surface FSM flush (recovers from blur/tab-switch
 * pointer loss that a per-pointer CANCEL cannot cover).
 */
export interface Lifecycle {
  theme: ThemeMode;
  focused: boolean;
  visible: boolean;
}

/**
 * Host → WASM envelope. `version` is field 1 (fail-fast guard); the consumer
 * checks it against the CURRENT value FIRST and returns a mismatch error — no
 * migration branch, no version-sniffing. {gte:1} rejects the proto3 default 0.
 */
export interface HostToWasm {
  version: number;
  pointer?: PointerEvent | undefined;
  lifecycle?: Lifecycle | undefined;
}

/**
 * What the WASM hit-test found under the pointer. Identity travels as the
 * WidgetNode.uid (a uint32), never a name — no string, stays size-bound.
 */
export interface HoverState {
  /** ui_ast WidgetNode.uid; 0 = nothing hovered */
  hoveredUid: number;
  /** host shows an interactive affordance when true */
  interactive: boolean;
}

/** The cursor the WASM wants the host to render. */
export interface CursorRequest {
  cursor: CursorType;
}

/** WASM → host envelope. Same version-guard contract as HostToWasm. */
export interface WasmToHost {
  version: number;
  hover?: HoverState | undefined;
  cursor?: CursorRequest | undefined;
}

function createBasePointerEvent(): PointerEvent {
  return { phase: 0, kind: 0, pointerId: 0, x: 0, y: 0, eventTime: Long.UZERO };
}

export const PointerEvent: MessageFns<PointerEvent> = {
  encode(message: PointerEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.phase !== 0) {
      writer.uint32(8).int32(message.phase);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    if (message.pointerId !== 0) {
      writer.uint32(24).uint32(message.pointerId);
    }
    if (message.x !== 0) {
      writer.uint32(33).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(41).double(message.y);
    }
    if (!message.eventTime.equals(Long.UZERO)) {
      writer.uint32(48).uint64(message.eventTime.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PointerEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePointerEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.phase = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pointerId = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.x = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.y = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.eventTime = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PointerEvent {
    return {
      phase: isSet(object.phase) ? pointerPhaseFromJSON(object.phase) : 0,
      kind: isSet(object.kind) ? pointerKindFromJSON(object.kind) : 0,
      pointerId: isSet(object.pointerId)
        ? globalThis.Number(object.pointerId)
        : isSet(object.pointer_id)
        ? globalThis.Number(object.pointer_id)
        : 0,
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      eventTime: isSet(object.eventTime)
        ? Long.fromValue(object.eventTime)
        : isSet(object.event_time)
        ? Long.fromValue(object.event_time)
        : Long.UZERO,
    };
  },

  toJSON(message: PointerEvent): unknown {
    const obj: any = {};
    if (message.phase !== 0) {
      obj.phase = pointerPhaseToJSON(message.phase);
    }
    if (message.kind !== 0) {
      obj.kind = pointerKindToJSON(message.kind);
    }
    if (message.pointerId !== 0) {
      obj.pointerId = Math.round(message.pointerId);
    }
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (!message.eventTime.equals(Long.UZERO)) {
      obj.eventTime = (message.eventTime || Long.UZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PointerEvent>, I>>(base?: I): PointerEvent {
    return PointerEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PointerEvent>, I>>(object: I): PointerEvent {
    const message = createBasePointerEvent();
    message.phase = object.phase ?? 0;
    message.kind = object.kind ?? 0;
    message.pointerId = object.pointerId ?? 0;
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.eventTime = (object.eventTime !== undefined && object.eventTime !== null)
      ? Long.fromValue(object.eventTime)
      : Long.UZERO;
    return message;
  },
};

function createBaseLifecycle(): Lifecycle {
  return { theme: 0, focused: false, visible: false };
}

export const Lifecycle: MessageFns<Lifecycle> = {
  encode(message: Lifecycle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.theme !== 0) {
      writer.uint32(8).int32(message.theme);
    }
    if (message.focused !== false) {
      writer.uint32(16).bool(message.focused);
    }
    if (message.visible !== false) {
      writer.uint32(24).bool(message.visible);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lifecycle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLifecycle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.theme = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.focused = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.visible = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lifecycle {
    return {
      theme: isSet(object.theme) ? themeModeFromJSON(object.theme) : 0,
      focused: isSet(object.focused) ? globalThis.Boolean(object.focused) : false,
      visible: isSet(object.visible) ? globalThis.Boolean(object.visible) : false,
    };
  },

  toJSON(message: Lifecycle): unknown {
    const obj: any = {};
    if (message.theme !== 0) {
      obj.theme = themeModeToJSON(message.theme);
    }
    if (message.focused !== false) {
      obj.focused = message.focused;
    }
    if (message.visible !== false) {
      obj.visible = message.visible;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lifecycle>, I>>(base?: I): Lifecycle {
    return Lifecycle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lifecycle>, I>>(object: I): Lifecycle {
    const message = createBaseLifecycle();
    message.theme = object.theme ?? 0;
    message.focused = object.focused ?? false;
    message.visible = object.visible ?? false;
    return message;
  },
};

function createBaseHostToWasm(): HostToWasm {
  return { version: 0, pointer: undefined, lifecycle: undefined };
}

export const HostToWasm: MessageFns<HostToWasm> = {
  encode(message: HostToWasm, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.pointer !== undefined) {
      PointerEvent.encode(message.pointer, writer.uint32(18).fork()).join();
    }
    if (message.lifecycle !== undefined) {
      Lifecycle.encode(message.lifecycle, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HostToWasm {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHostToWasm();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pointer = PointerEvent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lifecycle = Lifecycle.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HostToWasm {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      pointer: isSet(object.pointer) ? PointerEvent.fromJSON(object.pointer) : undefined,
      lifecycle: isSet(object.lifecycle) ? Lifecycle.fromJSON(object.lifecycle) : undefined,
    };
  },

  toJSON(message: HostToWasm): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.pointer !== undefined) {
      obj.pointer = PointerEvent.toJSON(message.pointer);
    }
    if (message.lifecycle !== undefined) {
      obj.lifecycle = Lifecycle.toJSON(message.lifecycle);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HostToWasm>, I>>(base?: I): HostToWasm {
    return HostToWasm.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HostToWasm>, I>>(object: I): HostToWasm {
    const message = createBaseHostToWasm();
    message.version = object.version ?? 0;
    message.pointer = (object.pointer !== undefined && object.pointer !== null)
      ? PointerEvent.fromPartial(object.pointer)
      : undefined;
    message.lifecycle = (object.lifecycle !== undefined && object.lifecycle !== null)
      ? Lifecycle.fromPartial(object.lifecycle)
      : undefined;
    return message;
  },
};

function createBaseHoverState(): HoverState {
  return { hoveredUid: 0, interactive: false };
}

export const HoverState: MessageFns<HoverState> = {
  encode(message: HoverState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hoveredUid !== 0) {
      writer.uint32(8).uint32(message.hoveredUid);
    }
    if (message.interactive !== false) {
      writer.uint32(16).bool(message.interactive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HoverState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHoverState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hoveredUid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.interactive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HoverState {
    return {
      hoveredUid: isSet(object.hoveredUid)
        ? globalThis.Number(object.hoveredUid)
        : isSet(object.hovered_uid)
        ? globalThis.Number(object.hovered_uid)
        : 0,
      interactive: isSet(object.interactive) ? globalThis.Boolean(object.interactive) : false,
    };
  },

  toJSON(message: HoverState): unknown {
    const obj: any = {};
    if (message.hoveredUid !== 0) {
      obj.hoveredUid = Math.round(message.hoveredUid);
    }
    if (message.interactive !== false) {
      obj.interactive = message.interactive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HoverState>, I>>(base?: I): HoverState {
    return HoverState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HoverState>, I>>(object: I): HoverState {
    const message = createBaseHoverState();
    message.hoveredUid = object.hoveredUid ?? 0;
    message.interactive = object.interactive ?? false;
    return message;
  },
};

function createBaseCursorRequest(): CursorRequest {
  return { cursor: 0 };
}

export const CursorRequest: MessageFns<CursorRequest> = {
  encode(message: CursorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== 0) {
      writer.uint32(8).int32(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CursorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCursorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cursor = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CursorRequest {
    return { cursor: isSet(object.cursor) ? cursorTypeFromJSON(object.cursor) : 0 };
  },

  toJSON(message: CursorRequest): unknown {
    const obj: any = {};
    if (message.cursor !== 0) {
      obj.cursor = cursorTypeToJSON(message.cursor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CursorRequest>, I>>(base?: I): CursorRequest {
    return CursorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CursorRequest>, I>>(object: I): CursorRequest {
    const message = createBaseCursorRequest();
    message.cursor = object.cursor ?? 0;
    return message;
  },
};

function createBaseWasmToHost(): WasmToHost {
  return { version: 0, hover: undefined, cursor: undefined };
}

export const WasmToHost: MessageFns<WasmToHost> = {
  encode(message: WasmToHost, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.hover !== undefined) {
      HoverState.encode(message.hover, writer.uint32(18).fork()).join();
    }
    if (message.cursor !== undefined) {
      CursorRequest.encode(message.cursor, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WasmToHost {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWasmToHost();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hover = HoverState.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cursor = CursorRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WasmToHost {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      hover: isSet(object.hover) ? HoverState.fromJSON(object.hover) : undefined,
      cursor: isSet(object.cursor) ? CursorRequest.fromJSON(object.cursor) : undefined,
    };
  },

  toJSON(message: WasmToHost): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.hover !== undefined) {
      obj.hover = HoverState.toJSON(message.hover);
    }
    if (message.cursor !== undefined) {
      obj.cursor = CursorRequest.toJSON(message.cursor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WasmToHost>, I>>(base?: I): WasmToHost {
    return WasmToHost.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WasmToHost>, I>>(object: I): WasmToHost {
    const message = createBaseWasmToHost();
    message.version = object.version ?? 0;
    message.hover = (object.hover !== undefined && object.hover !== null)
      ? HoverState.fromPartial(object.hover)
      : undefined;
    message.cursor = (object.cursor !== undefined && object.cursor !== null)
      ? CursorRequest.fromPartial(object.cursor)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
