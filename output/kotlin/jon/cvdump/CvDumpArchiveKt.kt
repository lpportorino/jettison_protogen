// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: jon_cv_dump_archive.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package jon.cvdump;

@kotlin.jvm.JvmName("-initializecvDumpArchive")
public inline fun cvDumpArchive(block: jon.cvdump.CvDumpArchiveKt.Dsl.() -> kotlin.Unit): jon.cvdump.JonCvDumpArchive.CvDumpArchive =
  jon.cvdump.CvDumpArchiveKt.Dsl._create(jon.cvdump.JonCvDumpArchive.CvDumpArchive.newBuilder()).apply { block() }._build()
/**
 * ```
 * ONE capture-session archive — the whole self-description of a cv_dump bundle
 * in a single nested message, written to <bundle>/archive.pb.
 *
 * It replaces manifest.json, io_records/<stream>.bin and
 * telemetry/<table>.jsonl.zst + telemetry/schema.json. The FAT-safe-filename
 * problem those files created is gone with them: a redis stream key now rides
 * in StreamGroup.source verbatim, so nothing is lossily rewritten to reach a
 * vfat/exfat export drive.
 *
 * What deliberately stays OUTSIDE this message: the ring video. A dump can
 * carry gigabytes of VCAP segments (the day ring alone defaults to 42 x 64 MiB
 * — see parse_segments in mods/video_capture/video_capture.cpp), which no
 * in-memory proto can hold and which a 2 GiB message limit forbids outright.
 * Segments therefore stay byte-for-byte files under video/<channel>/, fully
 * described by VideoSegment.
 *
 * FIELD ORDER IS DELIBERATE: everything needed to triage a bundle sits at a
 * LOWER field number than `streams`, so a reader that stops at the first large
 * field has already seen the version, the identity and the verdict.
 * ```
 *
 * Protobuf type `jon.cvdump.CvDumpArchive`
 */
public object CvDumpArchiveKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: jon.cvdump.JonCvDumpArchive.CvDumpArchive.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: jon.cvdump.JonCvDumpArchive.CvDumpArchive.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): jon.cvdump.JonCvDumpArchive.CvDumpArchive = _builder.build()

    /**
     * ```
     * PERMANENTLY FIELD 1. See ArchiveFormatVersion for why, and for how a reader
     * recovers it without a schema. Never renumber, never reuse.
     * ```
     *
     * `.jon.cvdump.ArchiveFormatVersion version = 1;`
     */
    public var version: jon.cvdump.JonCvDumpArchive.ArchiveFormatVersion
      @JvmName("getVersion")
      get() = _builder.version
      @JvmName("setVersion")
      set(value) {
        _builder.version = value
      }
    public var versionValue: kotlin.Int
      @JvmName("getVersionValue")
      get() = _builder.versionValue
      @JvmName("setVersionValue")
      set(value) {
        _builder.versionValue = value
      }
    /**
     * ```
     * PERMANENTLY FIELD 1. See ArchiveFormatVersion for why, and for how a reader
     * recovers it without a schema. Never renumber, never reuse.
     * ```
     *
     * `.jon.cvdump.ArchiveFormatVersion version = 1;`
     */
    public fun clearVersion() {
      _builder.clearVersion()
    }

    /**
     * ```
     * The capture-session id — an eutropia-minted dashed UUID, or a harvester
     * UUIDv4 of the same shape (see provenance). Also the bundle directory name
     * and the cv_dump_sessions primary key.
     * ```
     *
     * `string id = 2 [(.buf.validate.field) = { ... }`
     */
    public var id: kotlin.String
      @JvmName("getId")
      get() = _builder.id
      @JvmName("setId")
      set(value) {
        _builder.id = value
      }
    /**
     * ```
     * The capture-session id — an eutropia-minted dashed UUID, or a harvester
     * UUIDv4 of the same shape (see provenance). Also the bundle directory name
     * and the cv_dump_sessions primary key.
     * ```
     *
     * `string id = 2 [(.buf.validate.field) = { ... }`
     */
    public fun clearId() {
      _builder.clearId()
    }

    /**
     * ```
     * When this archive was written (ISO-8601, RFC3339 with nanoseconds, UTC).
     * NOT the capture window — that is `window`.
     * ```
     *
     * `string generated_at = 3;`
     */
    public var generatedAt: kotlin.String
      @JvmName("getGeneratedAt")
      get() = _builder.generatedAt
      @JvmName("setGeneratedAt")
      set(value) {
        _builder.generatedAt = value
      }
    /**
     * ```
     * When this archive was written (ISO-8601, RFC3339 with nanoseconds, UTC).
     * NOT the capture window — that is `window`.
     * ```
     *
     * `string generated_at = 3;`
     */
    public fun clearGeneratedAt() {
      _builder.clearGeneratedAt()
    }

    /**
     * ```
     * The [t0,t1] capture window, in both clock domains.
     * ```
     *
     * `.jon.cvdump.CaptureWindow window = 4;`
     */
    public var window: jon.cvdump.JonCvDumpArchive.CaptureWindow
      @JvmName("getWindow")
      get() = _builder.window
      @JvmName("setWindow")
      set(value) {
        _builder.window = value
      }
    /**
     * ```
     * The [t0,t1] capture window, in both clock domains.
     * ```
     *
     * `.jon.cvdump.CaptureWindow window = 4;`
     */
    public fun clearWindow() {
      _builder.clearWindow()
    }
    /**
     * ```
     * The [t0,t1] capture window, in both clock domains.
     * ```
     *
     * `.jon.cvdump.CaptureWindow window = 4;`
     * @return Whether the window field is set.
     */
    public fun hasWindow(): kotlin.Boolean {
      return _builder.hasWindow()
    }

    public val CvDumpArchiveKt.Dsl.windowOrNull: jon.cvdump.JonCvDumpArchive.CaptureWindow?
      get() = _builder.windowOrNull

    /**
     * `.jon.cvdump.SessionProvenance provenance = 5;`
     */
    public var provenance: jon.cvdump.JonCvDumpArchive.SessionProvenance
      @JvmName("getProvenance")
      get() = _builder.provenance
      @JvmName("setProvenance")
      set(value) {
        _builder.provenance = value
      }
    public var provenanceValue: kotlin.Int
      @JvmName("getProvenanceValue")
      get() = _builder.provenanceValue
      @JvmName("setProvenanceValue")
      set(value) {
        _builder.provenanceValue = value
      }
    /**
     * `.jon.cvdump.SessionProvenance provenance = 5;`
     */
    public fun clearProvenance() {
      _builder.clearProvenance()
    }

    /**
     * ```
     * Free-text harvester note. Empty on a healthy harvest; on a failed one it
     * carries the failure REASON, and is frequently the only human-readable
     * record of why a bundle is unusable. Distinct from the operator's own note,
     * which lives in cv_dump_sessions.operator_note and never here.
     * ```
     *
     * `string note = 6;`
     */
    public var note: kotlin.String
      @JvmName("getNote")
      get() = _builder.note
      @JvmName("setNote")
      set(value) {
        _builder.note = value
      }
    /**
     * ```
     * Free-text harvester note. Empty on a healthy harvest; on a failed one it
     * carries the failure REASON, and is frequently the only human-readable
     * record of why a bundle is unusable. Distinct from the operator's own note,
     * which lives in cv_dump_sessions.operator_note and never here.
     * ```
     *
     * `string note = 6;`
     */
    public fun clearNote() {
      _builder.clearNote()
    }

    /**
     * ```
     * Which device, which boot, which deployed code produced this capture.
     * ```
     *
     * `.jon.cvdump.MachineIdentity machine = 7;`
     */
    public var machine: jon.cvdump.JonCvDumpArchive.MachineIdentity
      @JvmName("getMachine")
      get() = _builder.machine
      @JvmName("setMachine")
      set(value) {
        _builder.machine = value
      }
    /**
     * ```
     * Which device, which boot, which deployed code produced this capture.
     * ```
     *
     * `.jon.cvdump.MachineIdentity machine = 7;`
     */
    public fun clearMachine() {
      _builder.clearMachine()
    }
    /**
     * ```
     * Which device, which boot, which deployed code produced this capture.
     * ```
     *
     * `.jon.cvdump.MachineIdentity machine = 7;`
     * @return Whether the machine field is set.
     */
    public fun hasMachine(): kotlin.Boolean {
      return _builder.hasMachine()
    }

    public val CvDumpArchiveKt.Dsl.machineOrNull: jon.cvdump.JonCvDumpArchive.MachineIdentity?
      get() = _builder.machineOrNull

    /**
     * ```
     * Whether the capture is trustworthy, and every degrade flag behind that
     * verdict.
     * ```
     *
     * `.jon.cvdump.IntegrityReport integrity = 8;`
     */
    public var integrity: jon.cvdump.JonCvDumpArchive.IntegrityReport
      @JvmName("getIntegrity")
      get() = _builder.integrity
      @JvmName("setIntegrity")
      set(value) {
        _builder.integrity = value
      }
    /**
     * ```
     * Whether the capture is trustworthy, and every degrade flag behind that
     * verdict.
     * ```
     *
     * `.jon.cvdump.IntegrityReport integrity = 8;`
     */
    public fun clearIntegrity() {
      _builder.clearIntegrity()
    }
    /**
     * ```
     * Whether the capture is trustworthy, and every degrade flag behind that
     * verdict.
     * ```
     *
     * `.jon.cvdump.IntegrityReport integrity = 8;`
     * @return Whether the integrity field is set.
     */
    public fun hasIntegrity(): kotlin.Boolean {
      return _builder.hasIntegrity()
    }

    public val CvDumpArchiveKt.Dsl.integrityOrNull: jon.cvdump.JonCvDumpArchive.IntegrityReport?
      get() = _builder.integrityOrNull

    /**
     * An uninstantiable, behaviorless type to represent the field in
     * generics.
     */
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    public class VideoProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     */
     public val video: com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>
      @kotlin.jvm.JvmSynthetic
      get() = com.google.protobuf.kotlin.DslList(
        _builder.videoList
      )
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     * @param value The video to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addVideo")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.add(value: jon.cvdump.JonCvDumpArchive.VideoChannel) {
      _builder.addVideo(value)
    }
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     * @param value The video to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignVideo")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.plusAssign(value: jon.cvdump.JonCvDumpArchive.VideoChannel) {
      add(value)
    }
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     * @param values The video to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addAllVideo")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.addAll(values: kotlin.collections.Iterable<jon.cvdump.JonCvDumpArchive.VideoChannel>) {
      _builder.addAllVideo(values)
    }
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     * @param values The video to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignAllVideo")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.plusAssign(values: kotlin.collections.Iterable<jon.cvdump.JonCvDumpArchive.VideoChannel>) {
      addAll(values)
    }
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     * @param index The index to set the value at.
     * @param value The video to set.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("setVideo")
    public operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.set(index: kotlin.Int, value: jon.cvdump.JonCvDumpArchive.VideoChannel) {
      _builder.setVideo(index, value)
    }
    /**
     * ```
     * Per-channel ring-video description, ONE ENTRY PER PROMISED CHANNEL — a
     * channel that captured nothing appears here with no segments rather than
     * being dropped, so the promise stays visible. The segment BYTES are files;
     * this is their inventory plus the per-segment time window parsed out of the
     * VCAP records at harvest time.
     *
     * There is deliberately no separate `channels` list and no `total_bytes`:
     * both would be stored copies of what this field already determines, and a
     * stored aggregate is free to disagree with its own parts. Sum the segments.
     * ```
     *
     * `repeated .jon.cvdump.VideoChannel video = 9;`
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("clearVideo")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.VideoChannel, VideoProxy>.clear() {
      _builder.clearVideo()
    }


    /**
     * An uninstantiable, behaviorless type to represent the field in
     * generics.
     */
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    public class StreamsProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     */
     public val streams: com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>
      @kotlin.jvm.JvmSynthetic
      get() = com.google.protobuf.kotlin.DslList(
        _builder.streamsList
      )
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     * @param value The streams to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addStreams")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.add(value: jon.cvdump.JonCvDumpArchive.StreamGroup) {
      _builder.addStreams(value)
    }
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     * @param value The streams to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignStreams")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.plusAssign(value: jon.cvdump.JonCvDumpArchive.StreamGroup) {
      add(value)
    }
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     * @param values The streams to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("addAllStreams")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.addAll(values: kotlin.collections.Iterable<jon.cvdump.JonCvDumpArchive.StreamGroup>) {
      _builder.addAllStreams(values)
    }
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     * @param values The streams to add.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("plusAssignAllStreams")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.plusAssign(values: kotlin.collections.Iterable<jon.cvdump.JonCvDumpArchive.StreamGroup>) {
      addAll(values)
    }
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     * @param index The index to set the value at.
     * @param value The streams to set.
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("setStreams")
    public operator fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.set(index: kotlin.Int, value: jon.cvdump.JonCvDumpArchive.StreamGroup) {
      _builder.setStreams(index, value)
    }
    /**
     * ```
     * The io-record and telemetry lanes, ORDERED BY (kind, source) so all groups
     * of one kind are contiguous. Last field number on purpose: it is the bulk.
     * ```
     *
     * `repeated .jon.cvdump.StreamGroup streams = 10;`
     */
    @kotlin.jvm.JvmSynthetic
    @kotlin.jvm.JvmName("clearStreams")
    public fun com.google.protobuf.kotlin.DslList<jon.cvdump.JonCvDumpArchive.StreamGroup, StreamsProxy>.clear() {
      _builder.clearStreams()
    }

  }
}
@kotlin.jvm.JvmSynthetic
public inline fun jon.cvdump.JonCvDumpArchive.CvDumpArchive.copy(block: `jon.cvdump`.CvDumpArchiveKt.Dsl.() -> kotlin.Unit): jon.cvdump.JonCvDumpArchive.CvDumpArchive =
  `jon.cvdump`.CvDumpArchiveKt.Dsl._create(this.toBuilder()).apply { block() }._build()

public val jon.cvdump.JonCvDumpArchive.CvDumpArchiveOrBuilder.windowOrNull: jon.cvdump.JonCvDumpArchive.CaptureWindow?
  get() = if (hasWindow()) getWindow() else null

public val jon.cvdump.JonCvDumpArchive.CvDumpArchiveOrBuilder.machineOrNull: jon.cvdump.JonCvDumpArchive.MachineIdentity?
  get() = if (hasMachine()) getMachine() else null

public val jon.cvdump.JonCvDumpArchive.CvDumpArchiveOrBuilder.integrityOrNull: jon.cvdump.JonCvDumpArchive.IntegrityReport?
  get() = if (hasIntegrity()) getIntegrity() else null

