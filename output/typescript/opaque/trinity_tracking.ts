// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.29.2
// source: opaque/trinity_tracking.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/**
 * How position_z_m was obtained. NOT cosmetic: board-extent range and LRF range
 * differ by more than an order of magnitude at 50 m.
 */
export enum TrinityRangeSource {
  TRINITY_RANGE_SOURCE_UNSPECIFIED = 0,
  /** TRINITY_RANGE_SOURCE_BOARD_EXTENT - Monocular, from the board's apparent size. Degrades with range squared. */
  TRINITY_RANGE_SOURCE_BOARD_EXTENT = 1,
  /** TRINITY_RANGE_SOURCE_LRF - Direct LRF distance. Roughly range-independent; the mm-class option. */
  TRINITY_RANGE_SOURCE_LRF = 2,
  /** TRINITY_RANGE_SOURCE_FUSED - LRF range with board-derived lateral and orientation. */
  TRINITY_RANGE_SOURCE_FUSED = 3,
  UNRECOGNIZED = -1,
}

export function trinityRangeSourceFromJSON(object: any): TrinityRangeSource {
  switch (object) {
    case 0:
    case "TRINITY_RANGE_SOURCE_UNSPECIFIED":
      return TrinityRangeSource.TRINITY_RANGE_SOURCE_UNSPECIFIED;
    case 1:
    case "TRINITY_RANGE_SOURCE_BOARD_EXTENT":
      return TrinityRangeSource.TRINITY_RANGE_SOURCE_BOARD_EXTENT;
    case 2:
    case "TRINITY_RANGE_SOURCE_LRF":
      return TrinityRangeSource.TRINITY_RANGE_SOURCE_LRF;
    case 3:
    case "TRINITY_RANGE_SOURCE_FUSED":
      return TrinityRangeSource.TRINITY_RANGE_SOURCE_FUSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TrinityRangeSource.UNRECOGNIZED;
  }
}

export function trinityRangeSourceToJSON(object: TrinityRangeSource): string {
  switch (object) {
    case TrinityRangeSource.TRINITY_RANGE_SOURCE_UNSPECIFIED:
      return "TRINITY_RANGE_SOURCE_UNSPECIFIED";
    case TrinityRangeSource.TRINITY_RANGE_SOURCE_BOARD_EXTENT:
      return "TRINITY_RANGE_SOURCE_BOARD_EXTENT";
    case TrinityRangeSource.TRINITY_RANGE_SOURCE_LRF:
      return "TRINITY_RANGE_SOURCE_LRF";
    case TrinityRangeSource.TRINITY_RANGE_SOURCE_FUSED:
      return "TRINITY_RANGE_SOURCE_FUSED";
    case TrinityRangeSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Tracker state. THE PAYLOAD IS PUBLISHED WHENEVER THE TRACKER PROCESS IS UP,
 * including when it is not tracking — that is what IDLE is for, and it is the
 * reason a consumer can answer "are we tracking?" without inferring anything
 * from whether the payload arrived.
 *
 * ABSENCE AND IDLE ARE DIFFERENT FACTS, and conflating them is the failure this
 * enum is shaped to prevent:
 *   IDLE present  -> the tracker is up and deliberately not tracking.
 *   payload absent -> the PRODUCER is down, or has not published yet, or the
 *                     payload was dropped. It does NOT mean tracking is off.
 * A consumer that treats "no payload" as "not tracking" will report a crashed
 * tracker as a stopped one, which is the same reading for two states that need
 * opposite responses.
 */
export enum TrinityTrackingStatus {
  TRINITY_TRACKING_STATUS_UNSPECIFIED = 0,
  /** TRINITY_TRACKING_STATUS_LOCKED - Tracking, pose fields valid. */
  TRINITY_TRACKING_STATUS_LOCKED = 1,
  /** TRINITY_TRACKING_STATUS_SEARCHING - Searching — no board found this tick; pose fields are stale/unset. */
  TRINITY_TRACKING_STATUS_SEARCHING = 2,
  /**
   * TRINITY_TRACKING_STATUS_DEGRADED - Board found but too small / too oblique for a pose; position may be
   * approximate, orientation is NOT valid.
   */
  TRINITY_TRACKING_STATUS_DEGRADED = 3,
  /** TRINITY_TRACKING_STATUS_BOARD_MISMATCH - Tracker running but the requested board version is not the one detected. */
  TRINITY_TRACKING_STATUS_BOARD_MISMATCH = 4,
  /**
   * TRINITY_TRACKING_STATUS_IDLE - Up, and NOT tracking — awaiting StartTrackTrinity, or stopped by
   * StopTrackTrinity. Pose, sigma and observability fields are meaningless and
   * MUST NOT be read; only board_version and capture_time_ns stay valid.
   * This is the state a consumer polls to answer "are we tracking?".
   */
  TRINITY_TRACKING_STATUS_IDLE = 5,
  UNRECOGNIZED = -1,
}

export function trinityTrackingStatusFromJSON(object: any): TrinityTrackingStatus {
  switch (object) {
    case 0:
    case "TRINITY_TRACKING_STATUS_UNSPECIFIED":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_UNSPECIFIED;
    case 1:
    case "TRINITY_TRACKING_STATUS_LOCKED":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_LOCKED;
    case 2:
    case "TRINITY_TRACKING_STATUS_SEARCHING":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_SEARCHING;
    case 3:
    case "TRINITY_TRACKING_STATUS_DEGRADED":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_DEGRADED;
    case 4:
    case "TRINITY_TRACKING_STATUS_BOARD_MISMATCH":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_BOARD_MISMATCH;
    case 5:
    case "TRINITY_TRACKING_STATUS_IDLE":
      return TrinityTrackingStatus.TRINITY_TRACKING_STATUS_IDLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TrinityTrackingStatus.UNRECOGNIZED;
  }
}

export function trinityTrackingStatusToJSON(object: TrinityTrackingStatus): string {
  switch (object) {
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_UNSPECIFIED:
      return "TRINITY_TRACKING_STATUS_UNSPECIFIED";
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_LOCKED:
      return "TRINITY_TRACKING_STATUS_LOCKED";
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_SEARCHING:
      return "TRINITY_TRACKING_STATUS_SEARCHING";
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_DEGRADED:
      return "TRINITY_TRACKING_STATUS_DEGRADED";
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_BOARD_MISMATCH:
      return "TRINITY_TRACKING_STATUS_BOARD_MISMATCH";
    case TrinityTrackingStatus.TRINITY_TRACKING_STATUS_IDLE:
      return "TRINITY_TRACKING_STATUS_IDLE";
    case TrinityTrackingStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Full-precision pose of the Ring-Trinity golden fiducial board.
 * Injected into JonGUIState.opaque_payloads by the trinity tracker at track rate.
 *
 * WHY DOUBLES AND NOT NDC: the SAM tracking payloads carry NDC in  because a
 * bounding box is a screen artifact. This payload carries a METRIC POSE — the board's
 * position in metres and its orientation as a unit quaternion — because it is the
 * ground-truth judge other measurements are scored against. Quantising it to screen
 * coordinates would destroy the precision the board exists to provide.
 *
 * UUID: 019facc1-62a1-74a8-9a75-2c195213c3d8
 */
export interface TrinityTracking {
  /**
   * ─── identity of the physical target ────────────────────────────────────────
   * WHICH BOARD this pose refers to. This is DATA, not the payload's schema
   * version — JonOpaquePayload.version already carries that, and the two move
   * independently: a schema change does not reprint the board, and a new board
   * revision does not change this message's shape. Conflating them makes a
   * re-print indistinguishable from a wire-format bump.
   */
  boardVersion:
    | TrinityBoardVersion
    | undefined;
  /**
   * ─── when ───────────────────────────────────────────────────────────────────
   * CLOCK_BOOTTIME nanoseconds of the frame this pose was computed from — the
   * same clock as a cv-dump's capture_time_ns and manifest t0_boot_ns/t1_boot_ns,
   * so a pose joins against telemetry without a clock conversion.
   */
  captureTimeNs: Long;
  status: TrinityTrackingStatus;
  /**
   * ─── position: board origin in CAMERA frame, metres ─────────────────────────
   * Right-handed, +X right, +Y down, +Z forward along the optical axis.
   *
   * PRECISION, MEASURED RATHER THAN PROMISED. `double` is not the limit — at 10 m
   * it resolves to ~1.8e-12 mm, over-provisioned for millimetres by ~12 orders of
   * magnitude. The OPTICS are the limit, and they are ANISOTROPIC:
   *
   *   LATERAL (x, y) = sigma_px * range / focal_px. At 0.4 px centre localisation:
   *     day  (focal_px 3517):  1.1 mm @ 10 m,   5.7 mm @ 50 m
   *     heat (focal_px 1320):  3.0 mm @ 10 m,  15.2 mm @ 50 m
   *
   *   RANGE (z) from apparent SIZE is far worse, because dZ/Z = dS/S and the board
   *   subtends few pixels: 27 mm @ 10 m day, 676 mm @ 50 m day. It degrades with
   *   the SQUARE of range, since the extent shrinks as the error stays fixed.
   *
   * So mm-level is achievable LATERALLY on day at short range, and is NOT
   * achievable in RANGE from board extent alone at any useful distance. That
   * asymmetry is why sigma_range_m is separate from sigma_position_m, and why
   * range_source exists.
   */
  positionXM: number;
  positionYM: number;
  positionZM: number;
  /**
   * ─── orientation: unit quaternion, board -> camera ──────────────────────────
   * A QUATERNION AND NOT EULER ANGLES, deliberately: Euler loses a degree of
   * freedom at +/-90 degrees pitch (gimbal lock), and the board is routinely
   * viewed near-edge-on where that singularity is reachable. A consumer wanting
   * Euler converts; a producer emitting Euler cannot un-lose the DoF.
   * Normalised: |q| = 1 within 1e-9.
   */
  quatW: number;
  quatX: number;
  quatY: number;
  quatZ: number;
  /**
   * ─── per-axis confidence, because one scalar cannot say what is true ────────
   * ROBUSTNESS AND PRECISION ARE DIFFERENT ORDERINGS HERE, and a consumer needs
   * both. By ROBUSTNESS at far range: lateral and range stay OBSERVABLE while
   * full 3-DoF orientation does not (the near-affine two-fold ambiguity). By
   * PRECISION: lateral is best, range is ~20x worse, orientation worst. A single
   * confidence number would have to lie about at least one axis, so consumers
   * gate on the axis they actually use.
   * 1-sigma uncertainties; negative means "not estimated".
   */
  sigmaPositionM: number;
  /** 1-sigma on Z alone. When range_source is */
  sigmaRangeM: number;
  /**
   * BOARD_EXTENT this is MUCH LARGER than
   * sigma_position_m (27 mm vs 1.1 mm at 10 m
   * day); with LRF it can be smaller. Never
   * assume the relation — read range_source.
   */
  sigmaOrientationMrad: number;
  /**
   * ─── the IPPE two-fold ambiguity, surfaced rather than hidden ───────────────
   * A planar target under near-affine projection admits TWO poses that
   * reprojection error cannot separate. A payload that emits only the chosen one
   * is silently wrong about half the time at range. The alternate is carried so a
   * consumer can see the fork and apply its own prior.
   */
  ambiguityResolved: boolean;
  /** unset when only one solution exists */
  alternate:
    | TrinityAltPose
    | undefined;
  /**
   * ─── where range came from ──────────────────────────────────────────────────
   * Range from board extent is the weakest axis (see position_z_m). The platform
   * carries an LRF whose distance is direct and roughly range-independent, so a
   * fused pose can be mm-class in Z where a vision-only pose cannot. A consumer
   * MUST be able to tell which it received: the two differ by more than an order
   * of magnitude at 50 m, and they look identical on the wire.
   */
  rangeSource: TrinityRangeSource;
  /** ─── observability, for judging the pose rather than trusting it ──────────── */
  anchorsSeen: number;
  boardExtentPx: number;
  reprojectionRmsPx: number;
}

/** Which physical board revision this pose refers to. */
export interface TrinityBoardVersion {
  /** Board family, e.g. "ring-trinity" */
  family: string;
  major: number;
  minor: number;
  /**
   * sha256 of the board's geometry manifest (boards/<board>.json). The manifest
   * is the one home for every board dimension, so its hash pins the geometry a
   * pose was computed against EXACTLY — a reprint from an edited manifest is a
   * different board and this field says so.
   */
  geometrySha256: string;
}

/** The pose the disambiguator did NOT choose. */
export interface TrinityAltPose {
  positionXM: number;
  positionYM: number;
  positionZM: number;
  quatW: number;
  quatX: number;
  quatY: number;
  quatZ: number;
  reprojectionRmsPx: number;
}

function createBaseTrinityTracking(): TrinityTracking {
  return {
    boardVersion: undefined,
    captureTimeNs: Long.UZERO,
    status: 0,
    positionXM: 0,
    positionYM: 0,
    positionZM: 0,
    quatW: 0,
    quatX: 0,
    quatY: 0,
    quatZ: 0,
    sigmaPositionM: 0,
    sigmaRangeM: 0,
    sigmaOrientationMrad: 0,
    ambiguityResolved: false,
    alternate: undefined,
    rangeSource: 0,
    anchorsSeen: 0,
    boardExtentPx: 0,
    reprojectionRmsPx: 0,
  };
}

export const TrinityTracking: MessageFns<TrinityTracking> = {
  encode(message: TrinityTracking, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.boardVersion !== undefined) {
      TrinityBoardVersion.encode(message.boardVersion, writer.uint32(10).fork()).join();
    }
    if (!message.captureTimeNs.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.captureTimeNs.toString());
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.positionXM !== 0) {
      writer.uint32(33).double(message.positionXM);
    }
    if (message.positionYM !== 0) {
      writer.uint32(41).double(message.positionYM);
    }
    if (message.positionZM !== 0) {
      writer.uint32(49).double(message.positionZM);
    }
    if (message.quatW !== 0) {
      writer.uint32(57).double(message.quatW);
    }
    if (message.quatX !== 0) {
      writer.uint32(65).double(message.quatX);
    }
    if (message.quatY !== 0) {
      writer.uint32(73).double(message.quatY);
    }
    if (message.quatZ !== 0) {
      writer.uint32(81).double(message.quatZ);
    }
    if (message.sigmaPositionM !== 0) {
      writer.uint32(89).double(message.sigmaPositionM);
    }
    if (message.sigmaRangeM !== 0) {
      writer.uint32(97).double(message.sigmaRangeM);
    }
    if (message.sigmaOrientationMrad !== 0) {
      writer.uint32(105).double(message.sigmaOrientationMrad);
    }
    if (message.ambiguityResolved !== false) {
      writer.uint32(112).bool(message.ambiguityResolved);
    }
    if (message.alternate !== undefined) {
      TrinityAltPose.encode(message.alternate, writer.uint32(122).fork()).join();
    }
    if (message.rangeSource !== 0) {
      writer.uint32(152).int32(message.rangeSource);
    }
    if (message.anchorsSeen !== 0) {
      writer.uint32(128).uint32(message.anchorsSeen);
    }
    if (message.boardExtentPx !== 0) {
      writer.uint32(137).double(message.boardExtentPx);
    }
    if (message.reprojectionRmsPx !== 0) {
      writer.uint32(145).double(message.reprojectionRmsPx);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrinityTracking {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrinityTracking();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.boardVersion = TrinityBoardVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.captureTimeNs = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.positionXM = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.positionYM = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.positionZM = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.quatW = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.quatX = reader.double();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.quatY = reader.double();
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.quatZ = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 89) {
            break;
          }

          message.sigmaPositionM = reader.double();
          continue;
        }
        case 12: {
          if (tag !== 97) {
            break;
          }

          message.sigmaRangeM = reader.double();
          continue;
        }
        case 13: {
          if (tag !== 105) {
            break;
          }

          message.sigmaOrientationMrad = reader.double();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.ambiguityResolved = reader.bool();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.alternate = TrinityAltPose.decode(reader, reader.uint32());
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.rangeSource = reader.int32() as any;
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.anchorsSeen = reader.uint32();
          continue;
        }
        case 17: {
          if (tag !== 137) {
            break;
          }

          message.boardExtentPx = reader.double();
          continue;
        }
        case 18: {
          if (tag !== 145) {
            break;
          }

          message.reprojectionRmsPx = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrinityTracking {
    return {
      boardVersion: isSet(object.boardVersion)
        ? TrinityBoardVersion.fromJSON(object.boardVersion)
        : isSet(object.board_version)
        ? TrinityBoardVersion.fromJSON(object.board_version)
        : undefined,
      captureTimeNs: isSet(object.captureTimeNs)
        ? Long.fromValue(object.captureTimeNs)
        : isSet(object.capture_time_ns)
        ? Long.fromValue(object.capture_time_ns)
        : Long.UZERO,
      status: isSet(object.status) ? trinityTrackingStatusFromJSON(object.status) : 0,
      positionXM: isSet(object.positionXM)
        ? globalThis.Number(object.positionXM)
        : isSet(object.position_x_m)
        ? globalThis.Number(object.position_x_m)
        : 0,
      positionYM: isSet(object.positionYM)
        ? globalThis.Number(object.positionYM)
        : isSet(object.position_y_m)
        ? globalThis.Number(object.position_y_m)
        : 0,
      positionZM: isSet(object.positionZM)
        ? globalThis.Number(object.positionZM)
        : isSet(object.position_z_m)
        ? globalThis.Number(object.position_z_m)
        : 0,
      quatW: isSet(object.quatW)
        ? globalThis.Number(object.quatW)
        : isSet(object.quat_w)
        ? globalThis.Number(object.quat_w)
        : 0,
      quatX: isSet(object.quatX)
        ? globalThis.Number(object.quatX)
        : isSet(object.quat_x)
        ? globalThis.Number(object.quat_x)
        : 0,
      quatY: isSet(object.quatY)
        ? globalThis.Number(object.quatY)
        : isSet(object.quat_y)
        ? globalThis.Number(object.quat_y)
        : 0,
      quatZ: isSet(object.quatZ)
        ? globalThis.Number(object.quatZ)
        : isSet(object.quat_z)
        ? globalThis.Number(object.quat_z)
        : 0,
      sigmaPositionM: isSet(object.sigmaPositionM)
        ? globalThis.Number(object.sigmaPositionM)
        : isSet(object.sigma_position_m)
        ? globalThis.Number(object.sigma_position_m)
        : 0,
      sigmaRangeM: isSet(object.sigmaRangeM)
        ? globalThis.Number(object.sigmaRangeM)
        : isSet(object.sigma_range_m)
        ? globalThis.Number(object.sigma_range_m)
        : 0,
      sigmaOrientationMrad: isSet(object.sigmaOrientationMrad)
        ? globalThis.Number(object.sigmaOrientationMrad)
        : isSet(object.sigma_orientation_mrad)
        ? globalThis.Number(object.sigma_orientation_mrad)
        : 0,
      ambiguityResolved: isSet(object.ambiguityResolved)
        ? globalThis.Boolean(object.ambiguityResolved)
        : isSet(object.ambiguity_resolved)
        ? globalThis.Boolean(object.ambiguity_resolved)
        : false,
      alternate: isSet(object.alternate) ? TrinityAltPose.fromJSON(object.alternate) : undefined,
      rangeSource: isSet(object.rangeSource)
        ? trinityRangeSourceFromJSON(object.rangeSource)
        : isSet(object.range_source)
        ? trinityRangeSourceFromJSON(object.range_source)
        : 0,
      anchorsSeen: isSet(object.anchorsSeen)
        ? globalThis.Number(object.anchorsSeen)
        : isSet(object.anchors_seen)
        ? globalThis.Number(object.anchors_seen)
        : 0,
      boardExtentPx: isSet(object.boardExtentPx)
        ? globalThis.Number(object.boardExtentPx)
        : isSet(object.board_extent_px)
        ? globalThis.Number(object.board_extent_px)
        : 0,
      reprojectionRmsPx: isSet(object.reprojectionRmsPx)
        ? globalThis.Number(object.reprojectionRmsPx)
        : isSet(object.reprojection_rms_px)
        ? globalThis.Number(object.reprojection_rms_px)
        : 0,
    };
  },

  toJSON(message: TrinityTracking): unknown {
    const obj: any = {};
    if (message.boardVersion !== undefined) {
      obj.boardVersion = TrinityBoardVersion.toJSON(message.boardVersion);
    }
    if (!message.captureTimeNs.equals(Long.UZERO)) {
      obj.captureTimeNs = (message.captureTimeNs || Long.UZERO).toString();
    }
    if (message.status !== 0) {
      obj.status = trinityTrackingStatusToJSON(message.status);
    }
    if (message.positionXM !== 0) {
      obj.positionXM = message.positionXM;
    }
    if (message.positionYM !== 0) {
      obj.positionYM = message.positionYM;
    }
    if (message.positionZM !== 0) {
      obj.positionZM = message.positionZM;
    }
    if (message.quatW !== 0) {
      obj.quatW = message.quatW;
    }
    if (message.quatX !== 0) {
      obj.quatX = message.quatX;
    }
    if (message.quatY !== 0) {
      obj.quatY = message.quatY;
    }
    if (message.quatZ !== 0) {
      obj.quatZ = message.quatZ;
    }
    if (message.sigmaPositionM !== 0) {
      obj.sigmaPositionM = message.sigmaPositionM;
    }
    if (message.sigmaRangeM !== 0) {
      obj.sigmaRangeM = message.sigmaRangeM;
    }
    if (message.sigmaOrientationMrad !== 0) {
      obj.sigmaOrientationMrad = message.sigmaOrientationMrad;
    }
    if (message.ambiguityResolved !== false) {
      obj.ambiguityResolved = message.ambiguityResolved;
    }
    if (message.alternate !== undefined) {
      obj.alternate = TrinityAltPose.toJSON(message.alternate);
    }
    if (message.rangeSource !== 0) {
      obj.rangeSource = trinityRangeSourceToJSON(message.rangeSource);
    }
    if (message.anchorsSeen !== 0) {
      obj.anchorsSeen = Math.round(message.anchorsSeen);
    }
    if (message.boardExtentPx !== 0) {
      obj.boardExtentPx = message.boardExtentPx;
    }
    if (message.reprojectionRmsPx !== 0) {
      obj.reprojectionRmsPx = message.reprojectionRmsPx;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrinityTracking>, I>>(base?: I): TrinityTracking {
    return TrinityTracking.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrinityTracking>, I>>(object: I): TrinityTracking {
    const message = createBaseTrinityTracking();
    message.boardVersion = (object.boardVersion !== undefined && object.boardVersion !== null)
      ? TrinityBoardVersion.fromPartial(object.boardVersion)
      : undefined;
    message.captureTimeNs = (object.captureTimeNs !== undefined && object.captureTimeNs !== null)
      ? Long.fromValue(object.captureTimeNs)
      : Long.UZERO;
    message.status = object.status ?? 0;
    message.positionXM = object.positionXM ?? 0;
    message.positionYM = object.positionYM ?? 0;
    message.positionZM = object.positionZM ?? 0;
    message.quatW = object.quatW ?? 0;
    message.quatX = object.quatX ?? 0;
    message.quatY = object.quatY ?? 0;
    message.quatZ = object.quatZ ?? 0;
    message.sigmaPositionM = object.sigmaPositionM ?? 0;
    message.sigmaRangeM = object.sigmaRangeM ?? 0;
    message.sigmaOrientationMrad = object.sigmaOrientationMrad ?? 0;
    message.ambiguityResolved = object.ambiguityResolved ?? false;
    message.alternate = (object.alternate !== undefined && object.alternate !== null)
      ? TrinityAltPose.fromPartial(object.alternate)
      : undefined;
    message.rangeSource = object.rangeSource ?? 0;
    message.anchorsSeen = object.anchorsSeen ?? 0;
    message.boardExtentPx = object.boardExtentPx ?? 0;
    message.reprojectionRmsPx = object.reprojectionRmsPx ?? 0;
    return message;
  },
};

function createBaseTrinityBoardVersion(): TrinityBoardVersion {
  return { family: "", major: 0, minor: 0, geometrySha256: "" };
}

export const TrinityBoardVersion: MessageFns<TrinityBoardVersion> = {
  encode(message: TrinityBoardVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.family !== "") {
      writer.uint32(10).string(message.family);
    }
    if (message.major !== 0) {
      writer.uint32(16).uint32(message.major);
    }
    if (message.minor !== 0) {
      writer.uint32(24).uint32(message.minor);
    }
    if (message.geometrySha256 !== "") {
      writer.uint32(34).string(message.geometrySha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrinityBoardVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrinityBoardVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.family = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.major = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minor = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.geometrySha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrinityBoardVersion {
    return {
      family: isSet(object.family) ? globalThis.String(object.family) : "",
      major: isSet(object.major) ? globalThis.Number(object.major) : 0,
      minor: isSet(object.minor) ? globalThis.Number(object.minor) : 0,
      geometrySha256: isSet(object.geometrySha256)
        ? globalThis.String(object.geometrySha256)
        : isSet(object.geometry_sha256)
        ? globalThis.String(object.geometry_sha256)
        : "",
    };
  },

  toJSON(message: TrinityBoardVersion): unknown {
    const obj: any = {};
    if (message.family !== "") {
      obj.family = message.family;
    }
    if (message.major !== 0) {
      obj.major = Math.round(message.major);
    }
    if (message.minor !== 0) {
      obj.minor = Math.round(message.minor);
    }
    if (message.geometrySha256 !== "") {
      obj.geometrySha256 = message.geometrySha256;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrinityBoardVersion>, I>>(base?: I): TrinityBoardVersion {
    return TrinityBoardVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrinityBoardVersion>, I>>(object: I): TrinityBoardVersion {
    const message = createBaseTrinityBoardVersion();
    message.family = object.family ?? "";
    message.major = object.major ?? 0;
    message.minor = object.minor ?? 0;
    message.geometrySha256 = object.geometrySha256 ?? "";
    return message;
  },
};

function createBaseTrinityAltPose(): TrinityAltPose {
  return { positionXM: 0, positionYM: 0, positionZM: 0, quatW: 0, quatX: 0, quatY: 0, quatZ: 0, reprojectionRmsPx: 0 };
}

export const TrinityAltPose: MessageFns<TrinityAltPose> = {
  encode(message: TrinityAltPose, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.positionXM !== 0) {
      writer.uint32(9).double(message.positionXM);
    }
    if (message.positionYM !== 0) {
      writer.uint32(17).double(message.positionYM);
    }
    if (message.positionZM !== 0) {
      writer.uint32(25).double(message.positionZM);
    }
    if (message.quatW !== 0) {
      writer.uint32(33).double(message.quatW);
    }
    if (message.quatX !== 0) {
      writer.uint32(41).double(message.quatX);
    }
    if (message.quatY !== 0) {
      writer.uint32(49).double(message.quatY);
    }
    if (message.quatZ !== 0) {
      writer.uint32(57).double(message.quatZ);
    }
    if (message.reprojectionRmsPx !== 0) {
      writer.uint32(65).double(message.reprojectionRmsPx);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrinityAltPose {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrinityAltPose();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.positionXM = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.positionYM = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.positionZM = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.quatW = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.quatX = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.quatY = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.quatZ = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.reprojectionRmsPx = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrinityAltPose {
    return {
      positionXM: isSet(object.positionXM)
        ? globalThis.Number(object.positionXM)
        : isSet(object.position_x_m)
        ? globalThis.Number(object.position_x_m)
        : 0,
      positionYM: isSet(object.positionYM)
        ? globalThis.Number(object.positionYM)
        : isSet(object.position_y_m)
        ? globalThis.Number(object.position_y_m)
        : 0,
      positionZM: isSet(object.positionZM)
        ? globalThis.Number(object.positionZM)
        : isSet(object.position_z_m)
        ? globalThis.Number(object.position_z_m)
        : 0,
      quatW: isSet(object.quatW)
        ? globalThis.Number(object.quatW)
        : isSet(object.quat_w)
        ? globalThis.Number(object.quat_w)
        : 0,
      quatX: isSet(object.quatX)
        ? globalThis.Number(object.quatX)
        : isSet(object.quat_x)
        ? globalThis.Number(object.quat_x)
        : 0,
      quatY: isSet(object.quatY)
        ? globalThis.Number(object.quatY)
        : isSet(object.quat_y)
        ? globalThis.Number(object.quat_y)
        : 0,
      quatZ: isSet(object.quatZ)
        ? globalThis.Number(object.quatZ)
        : isSet(object.quat_z)
        ? globalThis.Number(object.quat_z)
        : 0,
      reprojectionRmsPx: isSet(object.reprojectionRmsPx)
        ? globalThis.Number(object.reprojectionRmsPx)
        : isSet(object.reprojection_rms_px)
        ? globalThis.Number(object.reprojection_rms_px)
        : 0,
    };
  },

  toJSON(message: TrinityAltPose): unknown {
    const obj: any = {};
    if (message.positionXM !== 0) {
      obj.positionXM = message.positionXM;
    }
    if (message.positionYM !== 0) {
      obj.positionYM = message.positionYM;
    }
    if (message.positionZM !== 0) {
      obj.positionZM = message.positionZM;
    }
    if (message.quatW !== 0) {
      obj.quatW = message.quatW;
    }
    if (message.quatX !== 0) {
      obj.quatX = message.quatX;
    }
    if (message.quatY !== 0) {
      obj.quatY = message.quatY;
    }
    if (message.quatZ !== 0) {
      obj.quatZ = message.quatZ;
    }
    if (message.reprojectionRmsPx !== 0) {
      obj.reprojectionRmsPx = message.reprojectionRmsPx;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrinityAltPose>, I>>(base?: I): TrinityAltPose {
    return TrinityAltPose.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrinityAltPose>, I>>(object: I): TrinityAltPose {
    const message = createBaseTrinityAltPose();
    message.positionXM = object.positionXM ?? 0;
    message.positionYM = object.positionYM ?? 0;
    message.positionZM = object.positionZM ?? 0;
    message.quatW = object.quatW ?? 0;
    message.quatX = object.quatX ?? 0;
    message.quatY = object.quatY ?? 0;
    message.quatZ = object.quatZ ?? 0;
    message.reprojectionRmsPx = object.reprojectionRmsPx ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
