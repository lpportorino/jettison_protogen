---
title: Proto Documentation Index
type: index
---

# Proto Documentation

**Statistics:** {{stats.message-count}} messages, {{stats.enum-count}} enums, {{stats.field-count}} fields

## Messages by Package

{% for pkg in packages %}
### {{pkg.name}}

{% for msg in pkg.messages %}- [[proto/{{msg.id}}|{{msg.name}}]]{% if msg.short-description %} — {{msg.short-description}}{% endif %}
{% endfor %}
{% endfor %}

## Enums

{% for enum in enums %}- [[proto/{{enum.id}}|{{enum.name}}]]{% if enum.short-description %} — {{enum.short-description}}{% endif %}
{% endfor %}
