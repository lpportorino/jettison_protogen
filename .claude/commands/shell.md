---
description: Open interactive shell in Docker container
---

Open an interactive bash shell inside the protogen Docker container for debugging.

**Useful for:**
- Debugging generation issues
- Testing protoc commands manually
- Inspecting installed tools

**Usage:**
```
/shell
```

**Note:** This opens an interactive session. Exit with `exit` or Ctrl+D.

```bash
make shell
```
