// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: ui/ui_ast.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package ui;

@kotlin.jvm.JvmName("-initializetargetBox")
public inline fun targetBox(block: ui.TargetBoxKt.Dsl.() -> kotlin.Unit): ui.UiAst.TargetBox =
  ui.TargetBoxKt.Dsl._create(ui.UiAst.TargetBox.newBuilder()).apply { block() }._build()
/**
 * ```
 * One rectangle drawn by a WIDGET_TARGET_OVERLAY node — a computer-vision
 * detection or track, not an interactive control.
 * ```
 *
 * Protobuf type `ui.TargetBox`
 */
public object TargetBoxKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: ui.UiAst.TargetBox.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: ui.UiAst.TargetBox.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): ui.UiAst.TargetBox = _builder.build()

    /**
     * ```
     * Rect in DESIGN PIXELS, relative to the overlay's own content-box origin.
     * Same unit and same origin as WidgetNode.x/y, deliberately: a producer that
     * can place a node can place a box, and a per-mille or normalized rect would
     * be the only unit in this vocabulary that is not raw design px. Signed
     * because a detection at the frame edge is legitimately partly outside — the
     * overlay clips its children, so a box hanging off an edge draws its visible
     * part and nothing more.
     * ```
     *
     * `int32 x = 1;`
     */
    public var x: kotlin.Int
      @JvmName("getX")
      get() = _builder.x
      @JvmName("setX")
      set(value) {
        _builder.x = value
      }
    /**
     * ```
     * Rect in DESIGN PIXELS, relative to the overlay's own content-box origin.
     * Same unit and same origin as WidgetNode.x/y, deliberately: a producer that
     * can place a node can place a box, and a per-mille or normalized rect would
     * be the only unit in this vocabulary that is not raw design px. Signed
     * because a detection at the frame edge is legitimately partly outside — the
     * overlay clips its children, so a box hanging off an edge draws its visible
     * part and nothing more.
     * ```
     *
     * `int32 x = 1;`
     */
    public fun clearX() {
      _builder.clearX()
    }

    /**
     * `int32 y = 2;`
     */
    public var y: kotlin.Int
      @JvmName("getY")
      get() = _builder.y
      @JvmName("setY")
      set(value) {
        _builder.y = value
      }
    /**
     * `int32 y = 2;`
     */
    public fun clearY() {
      _builder.clearY()
    }

    /**
     * `int32 w = 3;`
     */
    public var w: kotlin.Int
      @JvmName("getW")
      get() = _builder.w
      @JvmName("setW")
      set(value) {
        _builder.w = value
      }
    /**
     * `int32 w = 3;`
     */
    public fun clearW() {
      _builder.clearW()
    }

    /**
     * `int32 h = 4;`
     */
    public var h: kotlin.Int
      @JvmName("getH")
      get() = _builder.h
      @JvmName("setH")
      set(value) {
        _builder.h = value
      }
    /**
     * `int32 h = 4;`
     */
    public fun clearH() {
      _builder.clearH()
    }

    /**
     * ```
     * Caption drawn inside the box's top-left corner. Empty = no caption. It
     * takes its text color and font from the OVERLAY node, so a PROP_TEXT_COLOR
     * or PROP_TEXT_FONT in the overlay's style_groups is a screen's one handle on
     * every caption — the boxes and captions are renderer-built and no StyleGroup
     * addresses them directly. The renderer carries those two values down
     * EXPLICITLY: LVGL's own style inheritance does not reach that far, because
     * the box object between overlay and caption re-resolves the theme's own text
     * color and inheritance stops at the first ancestor that resolves.
     * ```
     *
     * `string label = 5 [(.buf.validate.field) = { ... }`
     */
    public var label: kotlin.String
      @JvmName("getLabel")
      get() = _builder.label
      @JvmName("setLabel")
      set(value) {
        _builder.label = value
      }
    /**
     * ```
     * Caption drawn inside the box's top-left corner. Empty = no caption. It
     * takes its text color and font from the OVERLAY node, so a PROP_TEXT_COLOR
     * or PROP_TEXT_FONT in the overlay's style_groups is a screen's one handle on
     * every caption — the boxes and captions are renderer-built and no StyleGroup
     * addresses them directly. The renderer carries those two values down
     * EXPLICITLY: LVGL's own style inheritance does not reach that far, because
     * the box object between overlay and caption re-resolves the theme's own text
     * color and inheritance stops at the first ancestor that resolves.
     * ```
     *
     * `string label = 5 [(.buf.validate.field) = { ... }`
     */
    public fun clearLabel() {
      _builder.clearLabel()
    }

    /**
     * ```
     * Box stroke color. Absent = the theme's border color for the overlay's own
     * class, which is what makes an unstyled box theme-correct in both families
     * (the ChartSeries.color convention). When present it colors the caption
     * too, so one classification reads as one thing.
     * ```
     *
     * `.ui.Color color = 6;`
     */
    public var color: ui.UiAst.Color
      @JvmName("getColor")
      get() = _builder.color
      @JvmName("setColor")
      set(value) {
        _builder.color = value
      }
    /**
     * ```
     * Box stroke color. Absent = the theme's border color for the overlay's own
     * class, which is what makes an unstyled box theme-correct in both families
     * (the ChartSeries.color convention). When present it colors the caption
     * too, so one classification reads as one thing.
     * ```
     *
     * `.ui.Color color = 6;`
     */
    public fun clearColor() {
      _builder.clearColor()
    }
    /**
     * ```
     * Box stroke color. Absent = the theme's border color for the overlay's own
     * class, which is what makes an unstyled box theme-correct in both families
     * (the ChartSeries.color convention). When present it colors the caption
     * too, so one classification reads as one thing.
     * ```
     *
     * `.ui.Color color = 6;`
     * @return Whether the color field is set.
     */
    public fun hasColor(): kotlin.Boolean {
      return _builder.hasColor()
    }

    public val TargetBoxKt.Dsl.colorOrNull: ui.UiAst.Color?
      get() = _builder.colorOrNull
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun ui.UiAst.TargetBox.copy(block: `ui`.TargetBoxKt.Dsl.() -> kotlin.Unit): ui.UiAst.TargetBox =
  `ui`.TargetBoxKt.Dsl._create(this.toBuilder()).apply { block() }._build()

public val ui.UiAst.TargetBoxOrBuilder.colorOrNull: ui.UiAst.Color?
  get() = if (hasColor()) getColor() else null

