// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v5.29.2
// source: jon_shared_data_drive.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import {
  JonGuiDataDriveProgram,
  jonGuiDataDriveProgramFromJSON,
  jonGuiDataDriveProgramToJSON,
  JonGuiDataDriveState,
  jonGuiDataDriveStateFromJSON,
  jonGuiDataDriveStateToJSON,
} from "./jon_shared_data_types";

/**
 * Status of the sandboxed drive programs (scan / POI / park) hosted by
 * eutropia's DriveHost. Published every state tick from the owning program's
 * OUTPUT status block; `rotary.is_scanning` and friends stay the scan-specific
 * surface, this message is the program-agnostic one.
 */
export interface JonGuiDataDrive {
  /** Program that currently owns the platform (NONE when idle). */
  program: JonGuiDataDriveProgram;
  /** Lifecycle state of that program. */
  state: JonGuiDataDriveState;
  /**
   * Program-defined phase index (park: 0 PRE, 1 EL_APPROACH, 2 AZ_SLEW,
   * 3 EL_DESCEND, 4 FORWARD; scan/poi: 0 idle, 1 moving, 2 lingering).
   */
  phase: number;
  /** Program-defined error code; 0 when no fault. */
  errorCode: number;
  /** POI slot being looked at (-1 when the POI program is not active). */
  poiIndex: number;
  /** True from EnterTransport receipt until the transport latch is forwarded. */
  parkInProgress: boolean;
  /**
   * Generation counter of the REST-derived tables (scan nodes, POI slots)
   * currently loaded into the programs' environment.
   */
  tablesGeneration: number;
  /**
   * Version code of the owning program's WASM module (major*10000 +
   * minor*100 + patch), 0 when no module is loaded.
   */
  wasmVersion: number;
  /**
   * Commands the host refused since the last program start (out-of-range,
   * non-finite, or emitted by a non-owner).
   */
  rejectedCommands: number;
}

function createBaseJonGuiDataDrive(): JonGuiDataDrive {
  return {
    program: 0,
    state: 0,
    phase: 0,
    errorCode: 0,
    poiIndex: 0,
    parkInProgress: false,
    tablesGeneration: 0,
    wasmVersion: 0,
    rejectedCommands: 0,
  };
}

export const JonGuiDataDrive: MessageFns<JonGuiDataDrive> = {
  encode(message: JonGuiDataDrive, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.program !== 0) {
      writer.uint32(8).int32(message.program);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.phase !== 0) {
      writer.uint32(24).int32(message.phase);
    }
    if (message.errorCode !== 0) {
      writer.uint32(32).int32(message.errorCode);
    }
    if (message.poiIndex !== 0) {
      writer.uint32(40).int32(message.poiIndex);
    }
    if (message.parkInProgress !== false) {
      writer.uint32(48).bool(message.parkInProgress);
    }
    if (message.tablesGeneration !== 0) {
      writer.uint32(56).uint32(message.tablesGeneration);
    }
    if (message.wasmVersion !== 0) {
      writer.uint32(64).int32(message.wasmVersion);
    }
    if (message.rejectedCommands !== 0) {
      writer.uint32(72).uint32(message.rejectedCommands);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JonGuiDataDrive {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJonGuiDataDrive();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.program = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.state = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.phase = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.errorCode = reader.int32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.poiIndex = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.parkInProgress = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.tablesGeneration = reader.uint32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.wasmVersion = reader.int32();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.rejectedCommands = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): JonGuiDataDrive {
    return {
      program: isSet(object.program) ? jonGuiDataDriveProgramFromJSON(object.program) : 0,
      state: isSet(object.state) ? jonGuiDataDriveStateFromJSON(object.state) : 0,
      phase: isSet(object.phase) ? globalThis.Number(object.phase) : 0,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
      poiIndex: isSet(object.poiIndex)
        ? globalThis.Number(object.poiIndex)
        : isSet(object.poi_index)
        ? globalThis.Number(object.poi_index)
        : 0,
      parkInProgress: isSet(object.parkInProgress)
        ? globalThis.Boolean(object.parkInProgress)
        : isSet(object.park_in_progress)
        ? globalThis.Boolean(object.park_in_progress)
        : false,
      tablesGeneration: isSet(object.tablesGeneration)
        ? globalThis.Number(object.tablesGeneration)
        : isSet(object.tables_generation)
        ? globalThis.Number(object.tables_generation)
        : 0,
      wasmVersion: isSet(object.wasmVersion)
        ? globalThis.Number(object.wasmVersion)
        : isSet(object.wasm_version)
        ? globalThis.Number(object.wasm_version)
        : 0,
      rejectedCommands: isSet(object.rejectedCommands)
        ? globalThis.Number(object.rejectedCommands)
        : isSet(object.rejected_commands)
        ? globalThis.Number(object.rejected_commands)
        : 0,
    };
  },

  toJSON(message: JonGuiDataDrive): unknown {
    const obj: any = {};
    if (message.program !== 0) {
      obj.program = jonGuiDataDriveProgramToJSON(message.program);
    }
    if (message.state !== 0) {
      obj.state = jonGuiDataDriveStateToJSON(message.state);
    }
    if (message.phase !== 0) {
      obj.phase = Math.round(message.phase);
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    if (message.poiIndex !== 0) {
      obj.poiIndex = Math.round(message.poiIndex);
    }
    if (message.parkInProgress !== false) {
      obj.parkInProgress = message.parkInProgress;
    }
    if (message.tablesGeneration !== 0) {
      obj.tablesGeneration = Math.round(message.tablesGeneration);
    }
    if (message.wasmVersion !== 0) {
      obj.wasmVersion = Math.round(message.wasmVersion);
    }
    if (message.rejectedCommands !== 0) {
      obj.rejectedCommands = Math.round(message.rejectedCommands);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JonGuiDataDrive>, I>>(base?: I): JonGuiDataDrive {
    return JonGuiDataDrive.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JonGuiDataDrive>, I>>(object: I): JonGuiDataDrive {
    const message = createBaseJonGuiDataDrive();
    message.program = object.program ?? 0;
    message.state = object.state ?? 0;
    message.phase = object.phase ?? 0;
    message.errorCode = object.errorCode ?? 0;
    message.poiIndex = object.poiIndex ?? 0;
    message.parkInProgress = object.parkInProgress ?? false;
    message.tablesGeneration = object.tablesGeneration ?? 0;
    message.wasmVersion = object.wasmVersion ?? 0;
    message.rejectedCommands = object.rejectedCommands ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
